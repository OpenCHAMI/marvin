{"steps":[{"name":"Repository reconnaissance and auth model inventory","description":"Inspect the repository to map the current authentication, authorization, token issuance, metadata exposure, signing, validation, and testing architecture. Produce a concise inventory of relevant modules, request flows, extension points, and constraints. Explicitly identify the current client/caller authentication model at token endpoints, including supported auth methods, how caller identity is represented internally, and any existing authorization checks that would affect RFC 8693 token exchange.","requires_code":false,"expected_outputs":["Architecture inventory of token/auth-related components","List of relevant endpoints, services, models, and configuration surfaces","Summary of current client authentication and caller identity handling at token endpoints","Gap list against required features: configurable claims, service tokens, issuer metadata/JWKS, RFC 8693 exchange"],"success_criteria":["All token issuance and validation entry points are identified","Current client authentication model is explicitly documented","Dependencies and risks for implementing token exchange are listed","The inventory is detailed enough to support a concrete proposal without major unknowns"]},{"name":"Write phased Markdown proposal with standards mapping and acceptance criteria","description":"Create a Markdown design proposal that maps repository capabilities and intended changes to RFC 8693, issuer metadata/JWKS requirements, configurable claim handling, service identity issuance, and policy boundaries. The proposal should define scope, non-goals, phased implementation order, backward compatibility expectations, security considerations, and explicit acceptance criteria for each phase. It should also clarify whether metadata exposure will be OAuth authorization server metadata, OIDC discovery, or a partial subset, to avoid implying unsupported OIDC behavior.","requires_code":false,"expected_outputs":["Markdown proposal document","RFC 8693 mapping table","Per-phase acceptance criteria","Documented scope/non-goals and deferred items"],"success_criteria":["Proposal covers all in-scope deliverables and deferrals","Each phase has concrete acceptance criteria","Standards alignment and partial-support boundaries are explicit","Reviewers can approve implementation scope from the document alone"]},{"name":"Define token profile models and configurable claim-validation foundations","description":"Design and implement the foundational models for typed token profiles and configurable context/claim handling. Keep modeling and validation conceptually distinct even if implemented together: define token/profile types, claim schema/config structures, helper utilities, and bounded validation rules for required/optional claims, defaults, and project-scoped context behavior. Scope the first pass to the minimum needed for downstream issuance and exchange flows, avoiding overengineering.","requires_code":true,"expected_outputs":["Typed token/profile model definitions","Configuration structures for context and claim behavior","Claim parsing/validation helpers","Initial tests for model and validation behavior"],"success_criteria":["Token profile modeling is separate and understandable from claim-validation logic","Claim configuration supports required defaults and project-scoped context behavior","Validation rules reject malformed or unsafe inputs predictably","Tests cover both valid and invalid claim/config combinations"]},{"name":"Add issuer metadata, JWKS, and key identification support","description":"Implement or extend issuer metadata and JWKS exposure so external validators can discover signing material and issuer information. Add stable key identification (`kid`) support, ensure metadata accurately reflects actual capabilities, and explicitly distinguish between OAuth authorization server metadata and any OIDC discovery support. Update signing and verification paths as needed so emitted tokens can be validated via the published JWKS.","requires_code":true,"expected_outputs":["Issuer metadata endpoint or extended metadata response","JWKS endpoint with active signing keys","Key identifiers on signed tokens","Tests validating metadata and JWKS consistency"],"success_criteria":["Published metadata matches implemented capabilities","JWKS contains the keys needed to validate emitted tokens","Signed tokens include resolvable key identifiers","Interoperability tests can validate tokens using only issuer metadata and JWKS"]},{"name":"Implement project-scoped minting semantics with backward-compatible defaults","description":"Add issuance semantics for project-scoped or context-scoped tokens, using the configurable claim foundations established earlier. Preserve backward compatibility by defining how older clients behave when context claims are omitted and by documenting migration/default behavior. Ensure configuration can express prior defaults where needed so rollout does not break existing clients unexpectedly.","requires_code":true,"expected_outputs":["Updated issuance logic for project/context-scoped tokens","Backward compatibility rules for omitted or legacy context claims","Migration notes for old versus new minting behavior","Tests for defaulting and compatibility scenarios"],"success_criteria":["New scoped minting behavior works as configured","Legacy/omitted-context requests follow documented compatibility behavior","No unplanned claim regressions occur for existing clients","Compatibility tests pass for old and new request patterns"]},{"name":"Add policy evaluation interfaces with explicit failure taxonomy","description":"Introduce clear policy evaluation interfaces for issuance and exchange decisions, using deny-by-default behavior. The design must explicitly distinguish authentication failures, authorization denials, and missing/indeterminate policy data so responses, logs, and metrics are consistent. Ensure caller identity, subject token attributes, requested token parameters, and contextual claims can all be passed into policy evaluation cleanly.","requires_code":true,"expected_outputs":["Policy interface definitions and default implementations or stubs","Decision/result model covering allow, deny, and indeterminate states","Error mapping rules for authn/authz/policy-data failures","Tests for policy decision paths and failure classification"],"success_criteria":["Policy inputs include caller identity and token/request context","Deny-by-default behavior is enforced when policy is absent or indeterminate","Authentication, authorization, and missing-policy conditions are distinguishable","Logs and API responses can be generated consistently from decision results"]},{"name":"Implement or confirm service identity authentication and service token issuance","description":"Verify whether service/client authentication already exists; if not, implement the minimum required authentication prerequisite for service-driven issuance and token exchange. Then add service identity token issuance aligned with the token profile and policy foundations. Make dependencies explicit so this step does not assume a missing service authentication capability.","requires_code":true,"expected_outputs":["Documented service authentication prerequisite status","Service identity issuance flow or extension to existing flow","Configuration and policy hooks for service token issuance","Tests for authenticated service issuance scenarios"],"success_criteria":["It is explicit whether service authentication was pre-existing or newly added","Authenticated services can obtain service identity tokens through supported flows","Issuance respects policy and configured claims/profiles","Tests fail appropriately for unauthenticated or unauthorized services"]},{"name":"Implement RFC 8693 token exchange with explicit client authentication and OAuth-compatible errors","description":"Add the token exchange endpoint and service logic aligned with RFC 8693. Explicitly incorporate client authentication and caller authorization at the token endpoint, not just subject token validation. Define supported token type constants/registry values, validate subject/actor token parameters as applicable, enforce issuer/audience/lifetime checks, and determine whether exchanged tokens are access-token-like only or may also be refreshable. Return standards-compatible OAuth error fields and map internal authentication/authorization/policy failures consistently.","requires_code":true,"expected_outputs":["RFC 8693 token exchange endpoint and service implementation","Client authentication and caller authorization handling in exchange flow","Token type constants/registry mappings","OAuth-compatible error handling and response models","Tests for successful exchange and standards-compliant failure cases"],"success_criteria":["Exchange requests require and process authenticated callers as designed","Caller authorization and policy evaluation are enforced before issuance","Subject token validation includes issuer, audience, lifetime, and type checks","Error responses conform to expected OAuth/RFC 8693 fields and semantics","Tests cover success, invalid_request, invalid_target, invalid_grant/subject-token failure, unauthorized client/authn failure, and policy denial scenarios"]},{"name":"Document or scaffold extension points for delegated/envelope or proof-bound tokens","description":"Do not implement broad future-facing token mechanisms unless repository reality clearly requires them. Instead, add minimal documentation or seam-level scaffolding only if the existing codebase already contains delegated/envelope token concepts or a clear requirement for proof-bound tokens. Otherwise, capture these as deferred future work in proposal and code comments/interfaces without expanding scope.","requires_code":true,"expected_outputs":["Minimal extension interfaces or placeholders if justified by repository state","Documentation of deferred delegated/envelope/proof-bound support","Non-goal notes preventing accidental scope expansion"],"success_criteria":["No substantial implementation work is done for non-core features without justification","Any added scaffolding is minimal and does not complicate current flows","Deferred items are clearly documented for future work","Core deliverables remain the focus of implementation effort"]},{"name":"Add structured audit logging and revocation integration seams","description":"Implement structured audit logging for issuance, exchange decisions, authentication outcomes, authorization denials, policy failures, and key token attributes, while avoiding unsafe logging of secrets or sensitive payloads. Add revocation only as an interface or integration seam unless the repository already has token persistence or deny-list infrastructure. Ensure logs support operational review and incident analysis without overcommitting to a full revocation subsystem.","requires_code":true,"expected_outputs":["Structured audit log schema and instrumentation","Safe logging guidelines for sensitive fields","Revocation interface/hooks or integration seam","Tests or checks for log/event emission"],"success_criteria":["Core security-relevant events produce structured logs consistently","Sensitive data is not logged unsafely","Revocation scope stays limited to hooks unless existing infra supports more","Operators can distinguish authn, authz, policy, and validation outcomes from logs"]},{"name":"Expand tests, compatibility validation, interoperability checks, and final execution summary","description":"Build comprehensive unit, integration, and interoperability coverage for all implemented capabilities. Include compatibility validation for old/new claim names or defaults, behavior of existing tokens after deployment, rollout/rollback notes for config changes, and deployment assumptions introduced by issuer metadata/JWKS exposure. Add interoperability tests that validate emitted JWTs through the published issuer metadata and JWKS, not just local helpers. Conclude with a final execution summary describing what was implemented, what was deferred, how security criteria were addressed, and any remaining risks.","requires_code":true,"expected_outputs":["Expanded unit and integration test suites","Compatibility matrix and migration/rollout notes","JWT interoperability tests using published metadata/JWKS","Final execution summary with completed vs deferred work","Explicit security validation checklist results"],"success_criteria":["All implemented flows have automated tests with meaningful edge-case coverage","Backward-compatibility behavior is documented and validated","External-style JWT validation succeeds using exposed metadata and JWKS","Security checklist confirms no obvious privilege escalation, strict validation, bounded lifetimes, and safe logging","Final summary accurately reflects delivered scope, deferrals, and residual risks"]}]}
