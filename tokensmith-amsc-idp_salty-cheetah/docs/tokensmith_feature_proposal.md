{"steps":[{"name":"Repository inspection and dependency mapping","description":"Inspect the repository to identify current token issuance flows, request/response models, signing and key management, policy hooks, routing structure, configuration surfaces, and existing tests/docs. Map where user tokens, service tokens, project scoping, issuer metadata, and audit behavior already exist or are missing. Capture external contracts and likely downstream consumers to inform compatibility planning.","requires_code":true,"expected_outputs":["Repository architecture summary","Inventory of token-related modules, endpoints, configs, and tests","Dependency/risk notes for proposed enhancements","Initial list of existing API contracts and token consumer touchpoints"],"success_criteria":["All token-service entry points and issuance paths are identified","Key extension points for claims, policy, exchange, and metadata are documented","Compatibility-sensitive interfaces are listed for later verification"]},{"name":"Implementation proposal and MVP scope definition","description":"Draft a concise design proposal before coding that defines the target architecture, phased delivery order, and explicit MVP boundaries. Clarify which capabilities are in-scope for this iteration versus deferred, especially delegated/envelope tokens and proof-binding. Include data model changes, endpoint additions, policy semantics, compatibility expectations, and rollout considerations. This is a planning/documentation task; code changes are not required unless the proposal must be committed into the repository as documentation.","requires_code":false,"expected_outputs":["Design proposal document","MVP/in-scope vs deferred capability list","Dependency-aware implementation sequence","Open questions and approval checkpoints"],"success_criteria":["Proposal clearly distinguishes required features from optional/stretch items","Step sequencing aligns with implementation dependencies","Deferred items such as proof-binding or delegated/envelope support are explicitly gated if not required"]},{"name":"Token claim modeling and configurable claim handling","description":"Refine the token data model to support typed token profiles and configurable claims without breaking existing flows. Separate internal claim modeling from request validation if the current implementation is map-based. Define canonical claims, aliases or compatibility mappings for legacy names, configuration-driven optional claims, and validation rules for issuer, subject, audience, scope, project, and service identity attributes. Preserve backward compatibility where possible via aliases or config.","requires_code":true,"expected_outputs":["Typed token/profile models or equivalent structured claim layer","Configuration schema for claim inclusion/mapping","Validation rules for canonical and legacy claim paths","Migration notes for claim naming/interpretation"],"success_criteria":["Token creation uses structured claim handling rather than ad hoc maps where practical","Legacy claim names or shapes are preserved through aliases/config when needed","Validation rejects malformed or ambiguous claim combinations deterministically"]},{"name":"Policy interface refinement for typed issuance decisions","description":"Refine policy inputs and decision interfaces so issuance and exchange logic consume consistent typed context. Model principals, token type/profile, requested audiences/scopes, project scoping, service identity, and delegation/exchange context in a stable policy contract. Keep this focused on policy shape and decision boundaries, not all issuance behavior at once, so downstream minting and exchange steps can reuse it cleanly.","requires_code":true,"expected_outputs":["Updated policy request/decision interfaces","Policy evaluation rules or adapters for typed token context","Documentation of allow/deny/narrowing semantics","Unit tests for core policy decision inputs"],"success_criteria":["Issuance and exchange code can call a shared policy interface","Policy contracts cover user, service, and project-scoped contexts","Denial and narrowing outcomes are explicit and testable"]},{"name":"Service identity issuance and project-scoped minting","description":"Implement the main issuance tranche for service identities and project-scoped tokens using the new typed claim and policy layers. Define how service principals are authenticated/authorized for issuance, how project scoping is represented and validated, and how user versus service token semantics differ. Ensure both flows use consistent policy checks, claim generation, and configuration handling to avoid duplicated logic.","requires_code":true,"expected_outputs":["Updated issuance handlers/services for service identity tokens","Project-scoped token minting behavior","Shared validation and claim assembly paths for user/service issuance","Tests covering allowed, denied, and edge-case issuance scenarios"],"success_criteria":["Service tokens can be minted with correct claims and policy enforcement","Project scoping is consistently validated and encoded across issuance paths","Implementation avoids divergent logic between user and service issuance where shared behavior is expected"]},{"name":"Standardized error model and API response validation","description":"Define and implement a consistent error model across minting, policy denial, configuration validation, and token exchange-related endpoints. Use OAuth 2.0-style token endpoint errors where applicable, and stable validation error shapes/status codes elsewhere. Normalize error codes, messages, machine-readable fields, and mapping from internal failures to API responses. Add tests to lock response shapes and statuses.","requires_code":true,"expected_outputs":["Error taxonomy and response schema","Endpoint-specific error mapping rules","Tests for status codes and response bodies across failure modes","Documentation for client-visible error behavior"],"success_criteria":["Comparable failures return consistent status codes and response formats","OAuth-compliant endpoints emit appropriate standardized errors","Tests verify stable error shapes for validation, denial, and internal failure cases"]},{"name":"RFC 8693 token exchange protocol handling","description":"Implement the token exchange endpoint and request/response handling aligned with RFC 8693. Focus this step on protocol compliance: request parsing, grant type handling, subject token processing, actor/resource/audience parameter handling as supported, validation, response formatting, and integration with issuance services. Keep policy narrowing semantics delegated to the next step if needed, so protocol correctness remains reviewable on its own.","requires_code":true,"expected_outputs":["Token exchange endpoint/handler","Supported parameter and validation logic for RFC 8693 requests","Protocol-compliant success and error responses","Tests for valid/invalid exchange requests"],"success_criteria":["Exchange requests are parsed and validated according to supported RFC 8693 semantics","Successful responses contain the expected token response fields","Unsupported or invalid requests fail with documented standardized errors"]},{"name":"Token exchange policy, narrowing, and authorization semantics","description":"Layer authorization semantics onto token exchange by defining what exchanges are permitted, how scopes/audiences/projects are narrowed, whether actor/service context is retained, and how exchanged tokens differ from directly minted tokens. Reuse the typed policy interface to enforce least privilege and avoid privilege expansion. Document any intentionally unsupported RFC 8693 features.","requires_code":true,"expected_outputs":["Policy rules for exchange authorization and narrowing","Exchange-specific claim transformation logic","Tests covering privilege reduction, denial, and unsupported combinations","Documentation of supported vs unsupported exchange semantics"],"success_criteria":["Exchange cannot increase privileges beyond policy and input token constraints","Narrowing behavior is deterministic and test-covered","Supported exchange semantics are clearly documented for reviewers and clients"]},{"name":"Issuer metadata, JWKS, and key identifier exposure","description":"Expose or refine issuer metadata, JWKS, and key identifier behavior once token shapes and exchange capabilities are settled. Ensure metadata accurately reflects supported issuer/token endpoints and signing keys. Implement stable kid handling, signing key publication, and any required issuer discovery documents. Verify consistency between issued tokens, metadata, and verification expectations.","requires_code":true,"expected_outputs":["Issuer metadata endpoint(s) or updated metadata documents","JWKS endpoint or updated key publication logic","Stable key identifier generation/selection behavior","Tests validating metadata/JWKS consistency with issued tokens"],"success_criteria":["Published metadata matches implemented token and exchange endpoints","JWKS exposes active verification keys with correct identifiers","Issued tokens reference issuer and kid values that verifiers can resolve"]},{"name":"Operational logging, audit events, and revocation hooks","description":"Add or refine structured audit logging and revocation-related hooks for issuance and exchange flows. Include correlation/request IDs, event taxonomy, severity conventions, redaction rules for sensitive claims, and safeguards against PII leakage. Record key decisions such as mint, exchange, deny, revoke, and configuration errors without logging secrets or raw sensitive tokens. If full revocation is out of scope, add explicit hooks/scaffolding and document limitations.","requires_code":true,"expected_outputs":["Structured audit log/event definitions","Correlation ID and redaction behavior in token-related flows","Revocation hooks or documented partial implementation","Tests or checks for sensitive-data redaction and event emission"],"success_criteria":["Token events include correlation identifiers and consistent event names/severity","Sensitive claims/secrets/tokens are redacted or omitted from logs","Audit coverage exists for issuance, exchange, denial, and revocation-related operations as implemented"]},{"name":"Compatibility and migration verification","description":"Perform explicit backward-compatibility review and migration validation across updated token models, claims, endpoints, issuer metadata, and exchange behavior. Identify existing clients and token consumers, test legacy request flows against new code paths, preserve legacy behavior through aliases/config where necessary, and document operator/client migration guidance. This step should verify not just code correctness but safe adoption.","requires_code":true,"expected_outputs":["Compatibility matrix of old vs new behaviors","Regression tests for legacy flows","Migration notes for operators and client consumers","List of intentional breaking changes, if any, with mitigation guidance"],"success_criteria":["Previously supported core flows still work or have documented migration paths","Legacy clients are protected through compatibility settings or aliases where intended","Any breaking changes are explicit, justified, and documented"]},{"name":"Tests, documentation, and runnable examples","description":"Expand automated coverage and documentation for all implemented capabilities, including typed claims, service issuance, project scoping, standardized errors, token exchange, metadata/JWKS, logging behavior, and compatibility cases. Add example requests/responses and operational notes. If delegated/envelope tokens are deferred, document extension points and non-goals instead of implementing them in the main path.","requires_code":true,"expected_outputs":["Unit/integration/end-to-end tests","Updated API and operator documentation","Example configs and request/response samples","Documented extension points or deferred-feature notes"],"success_criteria":["Automated tests cover main success, denial, and error paths","Docs are sufficient for implementers, operators, and API consumers","Deferred items are clearly documented as out of scope or scaffolded only"]},{"name":"Final validation summary and approval package","description":"Compile a final summary that maps implemented work to the approved proposal, highlights MVP completion status, lists deferred items, summarizes compatibility outcomes, and captures residual risks. Include evidence from tests, example flows, and operational safeguards so reviewers can approve with clear traceability.","requires_code":false,"expected_outputs":["Final validation summary","Traceability matrix from plan to implementation","Residual risk and deferred-work list","Reviewer-ready approval notes"],"success_criteria":["Each in-scope requirement is mapped to evidence of completion","Deferred items such as proof-binding or delegated/envelope support are explicitly labeled","Reviewers can assess readiness, risks, and migration impact from a single summary"]}]}
