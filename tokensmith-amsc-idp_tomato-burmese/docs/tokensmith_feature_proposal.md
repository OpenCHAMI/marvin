{"steps":[{"name":"Repository inspection and architecture baseline","description":"Inspect the repository to identify existing token issuance and validation flows, HTTP routing, configuration loading, key management, policy enforcement hooks, and current claim structures. Produce a baseline summary of current endpoints, token formats, dependencies, and extension points that the implementation will rely on. This step should also identify any legacy token formats or claim conventions already in circulation so later work can preserve compatibility intentionally.","requires_code":false,"expected_outputs":["Architecture baseline summary","Inventory of token-related modules and endpoints","List of existing token claims, config knobs, and key management behavior","Compatibility notes covering legacy token formats and current API contracts"],"success_criteria":["All code paths involved in token minting, validation, exchange-like behavior, and auth policy are identified","Legacy token and claim handling expectations are documented","The baseline clearly identifies where additive changes can be introduced without breaking existing behavior"]},{"name":"Write Markdown implementation proposal","description":"Draft a Markdown proposal describing the intended architecture, phased delivery plan, data model changes, API additions, and standards alignment. The proposal should map the design to RFC 8693, explain compatibility guarantees, define what is in scope for phase 1 versus deferred, and call out migration behavior for tokens, claims, config, and endpoints. It should explicitly note that sender-constrained and delegated/envelope token work is optional or scaffold-only unless required.","requires_code":false,"expected_outputs":["Markdown proposal document","RFC 8693 capability matrix for phase 1","Scope table distinguishing required, optional, and deferred work","Compatibility and migration section"],"success_criteria":["The proposal includes explicit phase-1 scope boundaries","Compatibility behavior for legacy tokens, config, and endpoints is documented","Deferred items such as proof-bound or delegated token support are clearly marked to prevent scope creep"]},{"name":"Define compatibility, migration, and API error model","description":"Create a concrete compatibility and migration plan covering legacy token parsing/validation, claim-name migration behavior, config backward compatibility, and endpoint contract preservation. In the same step, define a standardized API error model for authentication, authorization, validation, and unsupported token exchange cases, including HTTP status codes, structured response payloads, and deterministic error codes/messages. This step provides the contract that later implementation and tests must satisfy.","requires_code":false,"expected_outputs":["Compatibility and migration specification","Claim migration decision table (read old/write new, dual-read, dual-write, etc.)","API error model specification with status codes and response schema","List of preserved, deprecated, and rerouted endpoints"],"success_criteria":["Migration behavior is explicitly defined for claims, config, and endpoints","Error handling distinguishes authentication, authorization, validation, and unsupported grant/type failures","The compatibility plan is detailed enough to derive implementation and tests directly"]},{"name":"Establish typed token profiles and validation foundation","description":"Introduce typed token profile structures and shared validation logic for the supported token classes. Refactor minting and verification code to use typed claims where possible while preserving backward-compatible parsing and validation of legacy token formats if they exist. Add a migration path for internal code that still consumes untyped claim maps, such as adapter layers or transitional interfaces, so the change remains additive and low risk.","requires_code":true,"expected_outputs":["Typed token profile definitions","Shared claim validation utilities","Backward-compatible parsing/validation for legacy tokens","Transitional adapters for code paths still using untyped claims"],"success_criteria":["Typed token structures cover all in-scope token classes","Existing valid legacy tokens continue to parse/validate where compatibility is required","Internal code has a documented migration path away from untyped claims","Validation behavior is consistent across minting and verification paths"]},{"name":"Add configurable project/accounting context claim support","description":"Implement support for configurable project or accounting context claims in token minting and validation. Make the migration behavior explicit according to the compatibility plan: for example, reading both old and new claim names, writing only the configured canonical claim, or dual-writing during a transition window. Ensure all policy and downstream consumers use normalized accessors so they do not depend on raw claim names.","requires_code":true,"expected_outputs":["Configurable claim-name support in token generation","Normalized claim access layer for consumers","Validation logic for old and new claim naming conventions","Tests covering migration modes"],"success_criteria":["Configured claim behavior matches the documented migration strategy exactly","Tokens with legacy and new claim names validate as intended","Downstream logic reads normalized context rather than hard-coded claim names","No endpoint contract is broken by the claim migration behavior"]},{"name":"Expose issuer metadata, JWKS, and key identification policy","description":"Implement or extend issuer metadata and JWKS endpoints and define key identification behavior for signed tokens. Explicitly document and enforce allowed signing algorithms, key selection rules, key rotation strategy, and behavior when multiple active keys exist. Ensure issued tokens include appropriate key identifiers and that validators can resolve signing keys reliably during overlap periods.","requires_code":true,"expected_outputs":["Issuer metadata endpoint or metadata document updates","JWKS endpoint or JWKS generation logic","Key ID assignment and token header updates","Documented signing algorithm and key rotation policy"],"success_criteria":["Metadata and JWKS responses are standards-aligned and internally consistent","Allowed signing algorithms are explicitly enforced","Tokens contain resolvable key identifiers","Validation works correctly during multi-key rotation windows"]},{"name":"Implement project-scoped minting and service token issuance","description":"Add or refine token issuance flows for project-scoped and service-oriented tokens using the typed token foundation and normalized claims. Define explicit TTL and lifetime rules by token type, and state whether existing mint endpoints are preserved, deprecated, or internally rerouted. Ensure issued tokens carry the right audience, subject, issuer, and context semantics needed for later exchange flows.","requires_code":true,"expected_outputs":["Updated minting services for project-scoped and service tokens","Token lifetime policy by token type","Endpoint compatibility behavior for existing mint APIs","Tests for claim content, lifetimes, and endpoint behavior"],"success_criteria":["Each token type has explicit and enforced lifetime rules","Existing mint endpoints behave according to the documented compatibility plan","Issued tokens contain correct typed claims and normalized project/accounting context","Minting paths remain additive and do not regress existing consumers"]},{"name":"Introduce explicit policy decision interfaces and deterministic deny-by-default handling","description":"Define and implement explicit policy decision interfaces for token issuance and exchange operations, defaulting to deny unless an operation is allowed. Standardize how policy denials are surfaced through the API layer so responses are deterministic, structured, and aligned with the error model. This should make authorization decisions auditable and prevent implicit allow behavior in edge cases.","requires_code":true,"expected_outputs":["Policy decision interfaces","Deny-by-default enforcement in issuance and exchange paths","Deterministic API-layer handling for policy denials","Tests for allowed, denied, and missing-policy scenarios"],"success_criteria":["No in-scope operation succeeds without an explicit allow path","Policy denials produce consistent status codes and structured error responses","Authorization logic is separated cleanly from transport and token parsing concerns","Tests cover both positive and negative decision paths"]},{"name":"Perform security and abuse-case review for token exchange","description":"Create a focused security checklist and review for the planned token exchange behavior before finalizing implementation. Cover audience confusion prevention, scope expansion prevention, replay considerations, token substitution risks, handling of untrusted subject tokens, validation of issuer/audience/token type prior to exchange, and any implications of actor semantics. Feed the review findings back into validation rules, policy checks, and error handling requirements.","requires_code":false,"expected_outputs":["Security review checklist","Threat/risk summary for token exchange","Required validation and policy hardening actions","Updated acceptance criteria for exchange implementation"],"success_criteria":["Major exchange-related abuse cases are explicitly analyzed","Each identified risk has a mitigation or documented deferral","Validation and policy requirements for exchange are tightened based on the review","No core exchange implementation proceeds with unresolved high-risk ambiguity"]},{"name":"Implement RFC 8693-aligned token exchange endpoint and service","description":"Implement the token exchange endpoint and backend service aligned with RFC 8693 for the defined phase-1 scope. Explicitly limit supported token types and grant combinations for the first implementation, define whether actor_token is supported now or scaffold-only, and enforce exact error behavior for unsupported combinations. The implementation should validate subject tokens rigorously, apply policy checks, constrain audience and scope transformations, and issue typed output tokens with auditable decisions.","requires_code":true,"expected_outputs":["Token exchange endpoint","Exchange service logic","Supported grant/token-type matrix for phase 1","Tests for valid exchanges, unsupported requests, and invalid subject tokens"],"success_criteria":["The endpoint implements the documented phase-1 subset of RFC 8693 consistently","Unsupported token types or parameter combinations return the standardized error responses","Subject token validation enforces issuer, audience, type, and trust requirements","Audience and scope cannot be expanded beyond policy and request constraints"]},{"name":"Add scaffold-only hooks for optional sender-constrained and delegated/envelope support","description":"Add minimal extension points for future sender-constrained, proof-bound, delegated, or envelope token support only if needed for architectural continuity. Do not implement full behavior unless it is a stated requirement; instead, add interfaces, placeholders, or design notes that avoid blocking future work while keeping current scope controlled. This step should remain intentionally lightweight.","requires_code":true,"expected_outputs":["Optional extension interfaces or stubs","Design notes for future sender-constrained/delegated support","Minimal tests if stubs are exposed publicly"],"success_criteria":["No substantial scope expansion occurs beyond scaffold-level work","Current functionality does not depend on unfinished optional features","Future extension points are clear without complicating the phase-1 implementation"]},{"name":"Add revocation primitives and structured audit logging","description":"Introduce minimal revocation groundwork and structured audit logging for token issuance and exchange events. Clarify whether this phase includes interface-only support or actual storage and lookup behavior for exchanged tokens, and keep implementation proportional to the current scope. Audit records should capture relevant identifiers, decisions, and reasons without leaking sensitive payload material.","requires_code":true,"expected_outputs":["Revocation interfaces and, if in scope, basic storage/lookup behavior","Structured audit log schema and emitted events","Tests for revocation checks and audit event generation","Documentation of revocation scope for this phase"],"success_criteria":["Revocation scope is explicit and implemented as documented","Audit logs are emitted for key issuance, denial, and exchange decisions","Sensitive data exposure in logs is minimized","Revocation and audit behavior are testable and deterministic"]},{"name":"Run compatibility, integration, and regression validation","description":"Execute a dedicated validation pass covering backward compatibility, migration behavior, endpoint contracts, and regression risk across the modified system. Verify that legacy tokens still validate where required, old configuration still loads safely, claim-name migration is deterministic, mint endpoints preserve their promised behavior, and new metadata/JWKS/exchange flows interoperate correctly. This step should combine automated tests with targeted manual verification where useful.","requires_code":true,"expected_outputs":["Compatibility and regression test results","Integration validation report","Manual verification notes for key migration and endpoint scenarios","List of any remaining compatibility gaps"],"success_criteria":["All documented compatibility guarantees are validated by tests or explicit checks","Legacy config and token behaviors work as promised","No unexpected regressions appear in existing minting or validation paths","Any remaining gaps are documented, bounded, and acceptable for release"]},{"name":"Update docs, examples, tests, and execution summary","description":"Finalize repository documentation, API docs, examples, configuration references, and test coverage to reflect the implemented behavior. Include examples for token issuance, metadata/JWKS discovery, and token exchange requests and responses, along with migration guidance and known phase-1 limitations. Produce a concise execution summary describing what was completed, what was deferred, and how the work was validated.","requires_code":true,"expected_outputs":["Updated README or docs pages","API and configuration reference updates","End-to-end examples and sample requests/responses","Execution summary with completed work, deferred items, and validation evidence"],"success_criteria":["Documentation matches implemented behavior and scope boundaries","Examples run or read consistently with the actual APIs","Deferred features and compatibility notes are clearly communicated","The final summary provides enough evidence for review and approval"]}]}
