{"steps":[{"name":"Repository inspection, baseline behavior capture, and compatibility inventory","description":"Inspect the repository to identify current token issuance, validation, configuration, endpoints, claim shapes, signing key handling, and authentication patterns. Capture the existing behavior with special attention to public API contracts, existing claim names, endpoint routes, config keys, and any current exchange-like or service-token flows. Produce a compatibility inventory that distinguishes behavior that must remain stable, behavior that can be extended safely, and items that may need explicit deprecation handling.","requires_code":true,"expected_outputs":["Repository architecture summary","Baseline behavior notes for issuance/validation/auth flows","Inventory of existing public endpoints, config keys, and claim contracts","Compatibility and deprecation risk list"],"success_criteria":["All relevant auth/token modules and entry points are identified","Current externally visible behavior is documented before changes begin","Potential breaking changes and migration-sensitive areas are explicitly listed","A clear set of backward-compatibility constraints is available for later steps"]},{"name":"Baseline tests and regression harness","description":"Create or tighten automated tests that capture current behavior for token minting, validation, endpoint responses, config loading, and error handling. These tests should serve as a regression harness so future changes can be validated against prior behavior where compatibility is required. Include compatibility-focused tests for preserved routes, claim names, and configuration semantics wherever applicable.","requires_code":true,"expected_outputs":["Baseline automated tests","Regression test matrix covering current critical flows","List of current gaps not yet test-covered"],"success_criteria":["Tests reproduce current expected behavior reliably","Critical existing flows are covered before implementation-heavy changes","Compatibility-sensitive behaviors are represented in tests","The suite can detect unintended regressions during later steps"]},{"name":"Define mandatory typed token and claim models","description":"Introduce typed models for the token families and claims that are required for the current scope. This should include the token profiles actually needed for issuance, validation, service identity handling, and RFC 8693 token exchange. Keep this step limited to mandatory models only; do not include optional delegated or envelope token scaffolds unless they are strictly required by the exchange/delegation work. Clearly define required claims, optional claims, validation rules, and serialization behavior.","requires_code":true,"expected_outputs":["Typed token profile definitions","Typed claim model definitions with validation rules","Mapping of existing claim usage to typed models"],"success_criteria":["All in-scope token families have explicit typed models","Required vs optional claims are unambiguous","Validation rules are enforceable in code","No speculative or out-of-scope token scaffolds are included in the mandatory model layer"]},{"name":"Configuration and feature-flag design with migration rules","description":"Define the configuration surface needed for issuer identity, signing keys, metadata/JWKS exposure, service-token settings, exchange policy controls, audit options, and revocation hooks. Include config migration and compatibility treatment: preserve existing keys where necessary, define aliases or deprecation paths for renamed settings, and document defaults. Ensure the design supports incremental rollout and safe disabling of new behavior if needed.","requires_code":true,"expected_outputs":["Configuration schema or definitions","Feature flags / rollout controls","Config compatibility and deprecation notes","Default-value and validation rules"],"success_criteria":["New configuration is explicit and validated","Existing config behavior is preserved or deprecated intentionally","Rollout controls allow safe enablement of new capabilities","Migration guidance exists for any config changes"]},{"name":"Issuer metadata and JWKS endpoints with explicit format definition","description":"Implement issuer metadata and JWKS exposure using a clearly defined format. Explicitly choose and document the metadata contract to expose, such as OAuth 2.0 Authorization Server Metadata, OpenID Connect Discovery, or a deliberately minimal metadata document if that is what the system supports. Define the minimum required fields, issuer consistency rules, key identifiers, cache behavior, and endpoint paths. Ensure JWKS content matches actual signing configuration and emitted token headers.","requires_code":true,"expected_outputs":["Metadata endpoint implementation","JWKS endpoint implementation","Documented metadata format and minimum fields","Issuer-to-key consistency checks"],"success_criteria":["Metadata format is explicitly identified and implemented","JWKS endpoint returns keys matching signing configuration","Issuer, kid, alg, and key material are internally consistent","Endpoint responses are testable and documented"]},{"name":"Token issuance and validation rules for typed claims and contexts","description":"Implement the core issuance and validation logic using the typed models. Define how claims are constructed, normalized, validated, and rejected across supported token contexts. Cover issuer/audience handling, time-based claims, subject representation, service identity distinctions, and any context-specific claim restrictions. Ensure the implementation does not blindly copy arbitrary claims and instead uses controlled construction rules.","requires_code":true,"expected_outputs":["Updated token issuance logic","Updated validation logic","Claim normalization and rejection rules","Tests for valid and invalid claim combinations"],"success_criteria":["Issued tokens conform to typed models","Validation rejects malformed or disallowed claims consistently","Context-specific claim rules are enforced","Claim construction is controlled rather than pass-through"]},{"name":"Authorization and claim translation policy boundary","description":"Define and implement the policy layer that determines what claims and rights may be issued, transformed, narrowed, or denied for each flow. This step should explicitly prohibit privilege expansion by default and define controlled claim translation rules, especially for claims derived from subject tokens or service identities. Document the authorization boundary between authentication inputs, token contents, and policy decisions.","requires_code":true,"expected_outputs":["Policy rules for issuance and exchange","Claim translation/narrowing rules","Documentation of privilege non-expansion defaults","Tests for allowed and denied policy outcomes"],"success_criteria":["Policy decisions are separated from token parsing/serialization logic","Privilege expansion is disallowed unless explicitly configured","Claim translation rules are documented and enforced","Tests confirm denial of unauthorized claim propagation or scope broadening"]},{"name":"Service identity and service-token issuance support","description":"Implement or refine service identity handling and service-token issuance using the typed model and policy boundary. Define how services authenticate, what claims identify a service principal, what audiences/scopes are allowed, and how service-issued tokens differ from end-user tokens. Ensure compatibility with existing repository authentication patterns and config where applicable.","requires_code":true,"expected_outputs":["Service principal claim mapping","Service-token issuance path","Authorization checks for service issuance","Tests for service-token success and failure cases"],"success_criteria":["Service identities are represented consistently","Service-token issuance follows explicit policy rules","Existing authentication patterns are reused or documented if extended","Tests validate both authorized and unauthorized service issuance behavior"]},{"name":"RFC 8693 token exchange implementation with explicit client auth and error model","description":"Implement RFC 8693-aligned token exchange behavior, including request parsing, subject token processing, optional actor token handling if feasible, audience/resource/scope handling, and output token minting under policy control. Explicitly document and enforce which client authentication methods are accepted for token exchange, how clients are authorized to perform exchange, and how claims are translated or narrowed. Define a consistent error model with standards-appropriate error codes, response structures, and HTTP status codes across exchange failures.","requires_code":true,"expected_outputs":["Token exchange endpoint/handler","RFC 8693 request-to-internal-model mapping","Client authentication and authorization rules for exchange","Error response contract and tests","Tests for claim narrowing and non-expansion behavior"],"success_criteria":["Exchange flow is implemented for in-scope RFC 8693 features","Accepted client auth methods are explicit and tested","Exchange never blindly copies claims or expands privilege by default","Error responses are consistent, standards-aligned, and covered by tests"]},{"name":"Delegation semantics and typed act claim support","description":"Add delegation semantics needed for exchanged or delegated contexts by introducing typed support for the act claim and related actor representation where feasible. Keep this step narrow: focus on modeling and issuing delegation-related claims required for the current RFC 8693 scope. If full actor token processing is practical, include it; otherwise document the supported subset. Explicitly defer envelope-token formats, encrypted delegated-token formats, or broader delegation packaging unless they are required by the problem scope.","requires_code":true,"expected_outputs":["Typed act claim model","Delegation claim construction and validation rules","Supported-subset documentation for actor/delegation handling","Tests for delegated/exchanged token claim semantics"],"success_criteria":["act claim semantics are implemented in a typed, testable way","Delegation-related claims are only included when policy and flow require them","Any unsupported actor/delegation features are explicitly documented","No out-of-scope envelope or encryption scaffolding is added to the core plan"]},{"name":"Minimal revocation hooks and structured audit logging","description":"Implement the minimum revocation-aware primitives and structured audit logging needed for safe operation without building a full revocation subsystem. This should include explicit interfaces or hooks for revocation checks, such as jti-based lookup or denylist integration points, along with clear no-op/default behavior if the subsystem is not fully wired. Add structured audit events for issuance, exchange, validation failures, and policy denials with careful treatment of sensitive data.","requires_code":true,"expected_outputs":["Revocation check interface/hook design","Minimal jti/denylist integration point or equivalent","Structured audit log schema/events","Tests for hook invocation and logging behavior"],"success_criteria":["Revocation scope is limited to defined hooks/interfaces rather than a full subsystem","Validation and/or issuance paths can call revocation-aware checks where intended","Audit logs capture security-relevant events consistently","Sensitive token material is not inappropriately exposed in logs"]},{"name":"Operational sanity checks, end-to-end validation, and documentation","description":"Run end-to-end validation across startup, configuration loading, metadata and JWKS exposure, issuance flows, service-token flows, exchange flows, delegation semantics, revocation hooks, logging, and backward-compatibility expectations. Perform lightweight operational sanity checks for endpoint exposure, startup correctness, and config behavior. Update technical documentation with architecture notes, supported standards/features, compatibility/deprecation guidance, configuration reference, error model, and rollout notes.","requires_code":true,"expected_outputs":["End-to-end validation results","Operational sanity-check notes","Updated architecture and API documentation","Compatibility/deprecation and rollout guidance"],"success_criteria":["All in-scope flows pass end-to-end tests","Operational checks show correct startup and endpoint behavior","Documentation matches the implemented behavior and limits","Compatibility promises and deprecations are clearly communicated"]}]}
