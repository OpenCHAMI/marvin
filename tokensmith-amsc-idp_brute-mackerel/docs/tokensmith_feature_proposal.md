{"steps":[{"name":"Inspect repository and restate scope","description":"Review the repository structure, existing auth/token code paths, configuration patterns, storage abstractions, HTTP routing conventions, testing setup, and documentation layout before proposing any changes. Identify whether issuer metadata, JWKS exposure, token mint/verify flows, policy hooks, and persistence/introspection primitives already exist. Produce a concise implementation-oriented summary that restates the requested objectives, constraints, assumptions, and any repo-specific limits discovered during inspection.","requires_code":false,"expected_outputs":["Repository inspection notes","List of existing relevant modules/endpoints/config patterns","Scoped problem statement with assumptions and risks"],"success_criteria":["All relevant auth/token/config/testing areas of the repo are identified","Known repo constraints are documented before implementation planning","The scoped summary matches the requested features without speculative additions"]},{"name":"Establish baseline behavior and reproducible test commands","description":"Run the current test suite and any available lint/typecheck/build commands to capture the starting state. Record the exact commands, environment requirements, and any failing or flaky tests that predate the planned work. This provides a reproducible baseline and helps ensure later changes preserve existing behavior unless intentionally modified.","requires_code":true,"expected_outputs":["Baseline test/lint/typecheck results","Recorded command list for reproducibility","List of pre-existing failures or warnings"],"success_criteria":["Core validation commands run successfully or existing failures are clearly documented","Exact commands and environment assumptions are recorded","A stable baseline exists for regression comparison"]},{"name":"Design token claim model and configuration/schema migration","description":"Define the canonical claim model for typed claims, configurable context-claim naming, issuer-related fields, project scoping, service identity claims, exchange/delegation claims, and any compatibility aliases needed for legacy consumers. In the same step, explicitly update the configuration schema: parser changes, defaults, env var and file-based support, backward-compatible config names where necessary, and startup-time validation for invalid issuer/context-claim settings. Specify which claims are authoritative only when signed in the token and how legacy claim parsing is preserved or intentionally changed.","requires_code":true,"expected_outputs":["Claim model/design notes","Updated config schema/parsing/default rules","Compatibility mapping for legacy claim/config names","Validation rules for startup-time config errors"],"success_criteria":["Claim names/types and authority rules are clearly defined","Configuration supports defaults, env/file inputs, and documented validation","Backward-compatibility behavior is explicit and testable","Invalid issuer/context-claim configuration fails predictably at startup"]},{"name":"Implement issuer metadata and JWKS exposure","description":"Add or extend issuer metadata and JWKS endpoints using repository conventions, including any .well-known routing if applicable. Ensure metadata fields align with the supported token features and configured issuer values. Reuse existing key management patterns where available, and avoid introducing unsupported key lifecycle mechanisms. Include compatibility checks so existing verification flows remain valid unless an intentional issuer-related change is documented.","requires_code":true,"expected_outputs":["Issuer metadata endpoint(s)","JWKS endpoint(s)","Configuration-driven issuer/key wiring","Metadata/JWKS tests"],"success_criteria":["Metadata and JWKS endpoints respond correctly and consistently","Configured issuer values are reflected accurately","Existing token verification flows still work or documented changes are intentional","Tests cover endpoint shape, key exposure, and compatibility behavior"]},{"name":"Implement typed claims and configurable signed context claim","description":"Update token minting and verification to use the typed claim model and support a configurable context claim name. Ensure that signed token claims are authoritative and that unsigned request-supplied context is never trusted over signed claims. Preserve legacy parsing behavior where required through aliases or transitional support, and add tests to confirm existing mint/verify consumers are not broken unintentionally.","requires_code":true,"expected_outputs":["Updated token mint/verify logic","Configurable context-claim support","Compatibility handling for legacy claims","Unit/integration tests for claim parsing and authority rules"],"success_criteria":["Tokens contain and parse typed claims correctly","Signed context claims override any conflicting unsigned inputs","Legacy claim consumers continue to work where required","Tests verify both new behavior and backward compatibility"]},{"name":"Add project-scoped minting and authorization policy interfaces","description":"Introduce project-scoped token issuance and the policy interfaces needed to allow or deny minting, exchange, delegation, and narrowing decisions. Keep the policy model deny-by-default and implement it as local synchronous logic or stable interfaces, depending on existing repo patterns; avoid overcommitting to an external policy engine unless already supported. Define how audience/resource/project constraints are narrowed and ensure policies cannot expand privilege by default.","requires_code":true,"expected_outputs":["Project-scoped minting flow","Policy interfaces/contracts","Default deny policy behavior","Tests for allowed/denied scope/project decisions"],"success_criteria":["Project-scoped issuance works with explicit constraints","Policy interfaces are clear and usable by later features","No privilege expansion occurs by default","Tests cover allow/deny and narrowing behavior"]},{"name":"Implement service token issuance","description":"Add support for service identity tokens distinct from end-user flows, following the typed claim and project/policy model already established. Define the service identity subject/audience semantics, ensure issuer and project constraints are applied consistently, and verify compatibility with existing verification and downstream consumers.","requires_code":true,"expected_outputs":["Service token minting support","Service-specific claim mapping","Tests for service token issuance and validation"],"success_criteria":["Service tokens can be minted and verified through supported flows","Service claims are well-defined and policy-controlled","Existing user token flows remain unaffected","Tests distinguish service and user semantics correctly"]},{"name":"Add proof-binding/sender-constrained scaffolding only","description":"If the requested problem includes future extensibility for sender-constrained or proof-bound tokens, implement only minimal claim-model or interface scaffolding needed to represent such bindings. Do not claim production sender-constrained support unless the repository already has the necessary verification mechanisms and tests. Keep this step tightly limited to non-speculative structure that unblocks future work without expanding scope.","requires_code":true,"expected_outputs":["Minimal interfaces or claim placeholders for proof-binding","Documentation/comments marking the feature as scaffolding if not fully implemented","Targeted tests only for implemented scaffolding behavior"],"success_criteria":["No speculative production feature is implied without full support","Scaffolding integrates cleanly with the token model","Tests confirm only the implemented interface/serialization behavior"]},{"name":"Implement RFC 8693 token exchange with strict error handling","description":"Add token exchange support aligned with RFC 8693 for the repository’s supported subset. Implement request validation, subject token validation, policy checks, audience/resource narrowing, token type handling, and issuance of exchanged tokens. Explicitly define and test error behavior for missing/invalid parameters, unsupported grant or token types, invalid subject tokens, forbidden exchanges, and consistent HTTP status/response body mapping. Enforce issuer and token-type validation strictly.","requires_code":true,"expected_outputs":["Token exchange endpoint/handler","RFC 8693 request/response mapping","Error handling matrix","Tests for success and failure cases"],"success_criteria":["Valid exchange requests succeed with correctly narrowed tokens","Invalid requests return consistent standards-aligned errors","Unsupported grant/token types are rejected predictably","Tests cover parameter validation, subject-token validation, policy denial, and HTTP/status/body consistency"]},{"name":"Implement delegation and act claim semantics","description":"Add delegation support centered on actor semantics such as the act claim, including rules for when and how actor identity is embedded during exchange or delegated issuance. Keep semantics explicit: delegation must not silently grant broader rights than the original subject and actor context permit. If full delegation enforcement is too large for the repo, implement the data model plus core validation hooks and document any deferred enforcement boundaries.","requires_code":true,"expected_outputs":["Delegation/actor claim handling","Validation rules for actor semantics","Tests for delegated identity representation and restrictions"],"success_criteria":["Actor/delegation claims are represented consistently","Delegation does not expand privileges by default","Validation rules are enforced or clearly scaffolded with boundaries documented","Tests cover basic delegated flows and denial cases"]},{"name":"Implement delegated or envelope token model if required","description":"If the repository requirements truly call for a distinct delegated or envelope token type beyond standard exchange/delegation claims, implement it here; otherwise combine this work with the previous step to reduce overhead. Keep the model clearly separated from RFC 8693 exchange semantics if both coexist. Define issuance, verification, and consumer expectations, and avoid introducing an unnecessary token form if existing claims satisfy the requirement.","requires_code":true,"expected_outputs":["Delegated/envelope token implementation or explicit consolidation with prior step","Verification rules for the token form","Tests for issuance/consumption semantics"],"success_criteria":["The token model is only introduced if justified by repo/problem requirements","Semantics are distinct and documented where needed","Tests validate issuance and verification behavior without overlap confusion"]},{"name":"Add logging, audit, and constrained revocation interfaces","description":"Implement structured logging and audit events for minting, exchange, delegation, denial, and key validation paths, ensuring no sensitive token values or secrets are written to logs. For revocation, clarify storage assumptions explicitly: if the repo already has suitable persistence or introspection patterns, integrate with them; otherwise add interface-only revocation hooks and no new datastore. Document what is actually enforced versus scaffolded.","requires_code":true,"expected_outputs":["Structured audit/logging additions","Revocation interfaces or integrations consistent with repo storage patterns","Documentation of enforced vs interface-only revocation behavior","Tests for logging/redaction and revocation hook behavior"],"success_criteria":["Logs contain actionable metadata without leaking token secrets","Revocation implementation matches existing repo storage capabilities","No new datastore is introduced unless already supported by repo patterns","Tests confirm redaction and interface/integration behavior"]},{"name":"Update documentation, examples, and migration notes","description":"Document configuration changes, claim formats, issuer metadata/JWKS endpoints, service tokens, project-scoped minting, exchange/delegation flows, revocation limitations, and any proof-binding scaffolding status. Include migration guidance for renamed or aliased config keys/claims, compatibility notes for existing consumers, and example requests/responses for core endpoints. Make clear which context values must be signed and authoritative.","requires_code":true,"expected_outputs":["Updated README/docs pages","Configuration and migration notes","Endpoint and token examples","Compatibility notes for existing consumers"],"success_criteria":["Documentation covers all implemented features and limitations","Migration/compatibility guidance is explicit and actionable","Examples align with actual behavior and tests"]},{"name":"Run full validation, compatibility checks, and security review","description":"Execute unit, integration, and regression tests across old and new flows. Add explicit backward-compatibility validation for legacy claim names, config names, and existing mint/verify behavior wherever compatibility was promised. Perform a lightweight security review focused on no privilege expansion by default, enforced audience/resource narrowing, strict issuer/token-type/actor validation, no trust in unsigned request context over signed claims, and no sensitive token values in logs. Record outcomes and any remaining gaps.","requires_code":true,"expected_outputs":["Full test results","Compatibility/regression test report","Security review checklist/results","Final issue list or accepted limitations"],"success_criteria":["All required tests pass or remaining failures are documented and justified","Legacy-compatible flows continue to work where promised","Security checks confirm narrowing, validation strictness, and log safety","Final results provide clear confidence in readiness"]}]}
