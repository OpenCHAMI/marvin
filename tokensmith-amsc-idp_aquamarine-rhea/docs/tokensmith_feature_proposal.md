{"steps":[{"name":"Repository inspection and baseline capture","description":"Inspect the repository to identify existing token models, signing/verification utilities, HTTP routing, configuration patterns, persistence/logging primitives, and any current compatibility constraints. Capture the current token formats, claim names, endpoints, error shapes, and dependency inventory for crypto, config, and web frameworks so later changes are grounded in the actual codebase.","requires_code":true,"expected_outputs":["Baseline inventory of token/auth-related modules","List of existing claims, endpoints, config keys, and error formats","Dependency inventory for signing, JWKS, HTTP, logging, and persistence","Risk list covering likely integration points and missing primitives"],"success_criteria":["All relevant auth/token code paths and dependencies are identified","Current behavior is documented well enough to compare before/after changes","Known constraints and assumptions are explicit and reviewable"]},{"name":"Write implementation proposal and scope decision record","description":"Produce a Markdown design/proposal document that consumes the baseline findings and locks the implementation scope before coding. The document should define required features, non-goals, phased delivery, compatibility constraints, API changes, and standards alignment. It should explicitly defer speculative work such as sender-constrained/proof-bound tokens unless they are a hard requirement, and narrow delegated/envelope support to extension-point scaffolding if full support is not mandatory.","requires_code":false,"expected_outputs":["Markdown proposal/design document","Explicit in-scope vs deferred feature list","Decision record for sender-constrained and delegated/envelope scope","Implementation sequence tied to repository realities"],"success_criteria":["Proposal is internally consistent and aligned with repository constraints","Deferred features are clearly marked to avoid overcommitment","Stakeholders can review a stable scope before implementation starts"]},{"name":"Define common typed token claims and validation foundation","description":"Implement the core typed claim models and validation layer for the required token profiles only. Cover common JWT claims, issuer/audience/subject handling, time-based claims, jti, actor/context hooks if required, and extension interfaces for profile-specific claims. Keep delegated/envelope-specific modeling out of this step except for explicit extension points so the foundation remains focused and reusable.","requires_code":true,"expected_outputs":["Typed common claim structures","Validation/parsing utilities for required claims","Extension interfaces or hooks for profile-specific claims","Unit tests for claim parsing and validation behavior"],"success_criteria":["Required claims can be serialized, parsed, and validated consistently","Invalid claims fail with deterministic validation results","Foundation is usable by minting, exchange, and service-token flows without duplication"]},{"name":"Review backward compatibility and migration strategy","description":"Define how new token behavior coexists with existing clients, validators, claim names, and already-issued tokens. Decide whether legacy claim names must still be emitted or accepted, how downstream consumers should migrate, what route behavior must remain stable, and what rollout or fallback mechanisms are needed. Convert these decisions into concrete compatibility rules that implementation steps must follow.","requires_code":false,"expected_outputs":["Compatibility matrix for old vs new token formats and APIs","Migration rules for claim names and validator expectations","Rollout notes for downstream consumers","Acceptance criteria for preserving backward-compatible behavior"],"success_criteria":["Existing-client compatibility expectations are explicit","Migration path exists for any renamed claims or changed responses","Implementation steps have measurable compatibility constraints to satisfy"]},{"name":"Add configurable project or accounting context claim support","description":"Implement configurable support for the project/accounting context claim, including naming, issuance rules, validation behavior, and precedence rules when values may come from signed token input versus request parameters. Ensure the behavior is compatible with the migration strategy and documented for operators and integrators.","requires_code":true,"expected_outputs":["Configurable context-claim implementation","Validation and precedence rules","Config documentation and examples","Tests covering enabled/disabled and legacy-name scenarios"],"success_criteria":["Context claim can be configured and validated as specified","Signed or trusted values take precedence where required","Legacy compatibility behavior matches the migration plan"]},{"name":"Expose issuer metadata, JWKS, and stable key identifiers","description":"Add standards-aligned issuer metadata and JWKS exposure, including stable kid handling and any required discovery metadata. Ensure issuer values, advertised signing keys, and runtime signing behavior are consistent, and account for rotation edge cases at the design and test level.","requires_code":true,"expected_outputs":["Issuer metadata endpoint or document","JWKS endpoint/output","kid assignment/selection logic","Tests for metadata correctness and key consistency"],"success_criteria":["Published metadata matches actual issuer and signing behavior","JWKS contains the expected active public keys","kid values resolve consistently during verification and rotation scenarios"]},{"name":"Implement project-scoped minting rules","description":"Add or refine minting logic so issued tokens are properly constrained to project/account scope according to policy and configuration. Enforce required scoping inputs, reject invalid widening of scope, and integrate the common claim foundation and context-claim support into issuance paths.","requires_code":true,"expected_outputs":["Project-scoped minting logic","Scope enforcement validations","Tests for allowed, denied, and edge-case minting requests","Updated implementation notes for scope behavior"],"success_criteria":["Minted tokens contain the expected project/account scope","Requests that exceed allowed scope are rejected","Scope behavior is consistent across token issuance paths"]},{"name":"Introduce explicit policy decision interfaces for minting and exchange","description":"Create a clear policy evaluation interface used by minting and token exchange flows to decide whether requested audiences, scopes, subjects, actors, and project contexts are allowed. Keep policy decisions explicit, testable, and separated from transport concerns so future policy backends can be added without rewriting endpoint logic.","requires_code":true,"expected_outputs":["Policy decision interfaces/contracts","Default policy implementation or adapter","Decision inputs/outputs documented","Tests for policy allow/deny behavior"],"success_criteria":["Minting and exchange paths rely on the policy interface instead of ad hoc checks","Policy decisions can be unit tested independently","Denied requests return policy-driven outcomes consistently"]},{"name":"Add service identity token issuance","description":"Implement issuance for service identity tokens using the common claims and project-scope rules. Define the required claims, allowed audiences/scopes, and any service-specific validation so non-human principals can obtain standards-aligned tokens without bypassing policy controls.","requires_code":true,"expected_outputs":["Service token issuance flow","Service-token claim profile","Tests for service identity issuance and denial cases","Operator/developer usage examples"],"success_criteria":["Service identities can obtain valid tokens under policy control","Issued service tokens carry the expected claims and constraints","Service issuance does not weaken existing user-token rules"]},{"name":"Standardize API and error contracts","description":"Define and implement consistent API response and error behavior across new and modified endpoints. Include HTTP status usage, machine-readable error bodies, validation error structure, and RFC 8693 error mappings for exchange-related failures. Ensure changes remain compatible with any existing route behavior that must be preserved.","requires_code":true,"expected_outputs":["Documented API/error contract","Shared error response structures or helpers","RFC 8693-aligned error mapping for exchange flows","Tests for status codes and response body consistency"],"success_criteria":["Endpoints return consistent status codes and structured errors","Exchange failures map to expected RFC 8693-style error responses","Backward-compatible route/error behavior is preserved where required"]},{"name":"Implement RFC 8693-aligned token exchange endpoint and service","description":"Build the token exchange endpoint and underlying service logic aligned to RFC 8693 for the scoped feature set. Support the required grant parameters, subject token validation, audience/resource handling, policy checks, claim transformation, and issuance of exchanged tokens. Keep the implementation limited to the approved scope and use extension points rather than full delegated/envelope implementations unless those are explicitly required.","requires_code":true,"expected_outputs":["Token exchange endpoint","Exchange service implementation","Request/response validation and transformation logic","Tests for successful and denied exchange scenarios"],"success_criteria":["Supported exchange requests succeed with valid issued tokens","Invalid or unauthorized exchanges fail with the defined error contract","Implementation matches the approved RFC 8693-aligned scope without speculative extras"]},{"name":"Add delegated or envelope token extension scaffolding if required","description":"Only if delegated or envelope-style tokens are explicitly in scope, add minimal typed extension points and validation hooks needed to support them later or in a narrow initial form. Do not implement nested encryption or a full confidential token subsystem unless the repository already supports it and the requirement is explicit. If not required, record the deferral in documentation instead of building code.","requires_code":true,"expected_outputs":["Minimal delegated/envelope extension interfaces or hooks, if in scope","Narrow validation/model additions, if required","Deferral note in docs if feature is postponed"],"success_criteria":["No unnecessary subsystem is built beyond approved scope","If implemented, scaffolding integrates cleanly with common claims and exchange flows","Deferred status is documented clearly when full support is not included"]},{"name":"Add revocation primitives and structured audit logging","description":"Implement the operational controls needed to trace and constrain token usage, including jti-aware revocation primitives as appropriate, structured audit events for issuance and exchange, and log redaction rules to avoid token leakage. Ensure events capture enough context for debugging and compliance without exposing secrets or overlogging claims.","requires_code":true,"expected_outputs":["Revocation-related primitives or interfaces","Structured audit log events for minting/exchange","Logging/redaction rules","Tests for audit emission and safe logging behavior"],"success_criteria":["Important issuance and exchange actions generate structured audit events","Sensitive token material is not leaked in logs","Revocation primitives support the intended operational checks"]},{"name":"Perform focused security review and threat validation","description":"Run a lightweight but explicit security review over the implemented design. Validate protections against privilege escalation, audience confusion, actor/delegation misuse, request-body override of trusted signed claims, JWKS/kid/issuer inconsistencies, and leakage through logs or errors. Convert findings into fixes or documented residual risks before closeout.","requires_code":true,"expected_outputs":["Security review checklist/results","List of discovered issues and fixes or accepted risks","Additional negative tests for identified threat cases"],"success_criteria":["High-risk privilege or token-handling flaws are addressed","Threat cases are covered by tests or documented mitigations","Residual risks are explicit and acceptable for the release scope"]},{"name":"Add tests, examples, and user-facing documentation","description":"Create comprehensive automated tests and concise user/operator documentation covering configuration, endpoints, token formats, compatibility expectations, migration notes, error responses, and example request/response flows. Ensure examples reflect the final scoped feature set and avoid documenting deferred capabilities as if they are implemented.","requires_code":true,"expected_outputs":["Unit/integration/end-to-end tests","Configuration and API documentation","Migration and compatibility notes","Example token flows and sample requests/responses"],"success_criteria":["Automated tests cover core success and failure paths","Documentation matches implemented behavior and scope","Users can configure and exercise the feature set from the docs alone"]},{"name":"Run final validation, reconcile plan drift, and write execution summary","description":"Execute the full validation pass, compare delivered work against the approved proposal, reconcile any plan drift, and summarize what shipped, what changed, what was deferred, and any remaining risks. Confirm compatibility, standards alignment, and operational readiness before handoff.","requires_code":true,"expected_outputs":["Final validation/test results","Plan-vs-implementation reconciliation note","Execution summary with shipped/deferred items and risks","Release-readiness checklist"],"success_criteria":["All required validation checks pass or have documented exceptions","Any divergence from plan is explained and justified","Final summary provides a clear handoff artifact for reviewers and operators"]}]}
