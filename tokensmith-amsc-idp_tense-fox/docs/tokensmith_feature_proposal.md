{"steps":[{"name":"Inspect repository and map current auth/token architecture","description":"Review the repository to identify existing authentication flows, token issuance/parsing code, claim representations, policy interfaces, metadata/JWKS support, HTTP routes, and test coverage. Capture how current client authentication and authorization patterns work, what token formats already exist, and where RFC 8693 support would attach with minimal disruption. This step should also identify constraints, reusable components, and any existing migration/versioning conventions.","requires_code":false,"expected_outputs":["Architecture inventory of current auth/token components","List of relevant files/modules/endpoints","Summary of existing client auth patterns and trust model","Gap analysis against RFC 8693 and related token-minting requirements"],"success_criteria":["All core auth/token modules and routes are identified","Existing client authentication patterns are documented clearly enough to reuse or reject for STS","Known constraints and integration points for implementation are recorded","Open questions are reduced to a small, explicit list"]},{"name":"Write implementation proposal and scope phases","description":"Produce a Markdown design/proposal document that defines the target behavior, MVP scope, Phase 2 items, and deferred items. Keep this step documentation-only. The proposal should include route/API additions, compatibility expectations, migration approach, and explicit decisions about what is out of scope for initial delivery. It should also record whether optional items such as sender-constrained tokens, delegated/envelope tokens, and deeper revocation support are deferred.","requires_code":false,"expected_outputs":["Markdown proposal/design document","Explicit MVP/Phase 2/Deferred scope breakdown","Decision log for optional features and unresolved questions","Implementation sequence aligned to repository structure"],"success_criteria":["Proposal is documentation-only with no code changes required","MVP scope is narrow and clearly separated from deferred features","Optional/speculative features have explicit go/no-go or defer decisions","Reviewers can determine planned API additions and compatibility posture from the document alone"]},{"name":"Review API surface and versioning compatibility","description":"Define the HTTP/API changes needed for token exchange and related metadata updates. Confirm whether endpoints are additive, whether any response shapes change, and whether versioning is required. Document route naming, request/response compatibility, error semantics, and any operator-visible changes. This should be completed before implementation to avoid accidental breaking changes.","requires_code":false,"expected_outputs":["API surface review note","Route and response compatibility matrix","Versioning decision and rationale","List of additive vs potentially breaking changes"],"success_criteria":["Every proposed endpoint/change is classified as additive or breaking","No unreviewed response-shape changes remain","Versioning strategy is explicitly documented","API reviewers can validate that MVP will not introduce unintended breaking changes"]},{"name":"Introduce typed token claims and validation foundation","description":"Implement a clear typed representation for token claims and the associated validation rules used across issuance and exchange flows. Establish canonical handling for standard claims and repo-specific custom claims, along with validation and serialization boundaries. Keep this step focused on typed claims and validation only, without mixing in configuration migration concerns.","requires_code":true,"expected_outputs":["Typed claim models/interfaces","Validation and serialization/parsing logic","Unit tests for valid/invalid claim combinations","Developer notes describing canonical claim handling"],"success_criteria":["Claims are represented through typed structures rather than ad hoc maps where targeted","Validation failures are deterministic and covered by tests","Existing supported claim behavior remains intact or differences are documented","This foundation can be reused by issuance and token exchange flows"]},{"name":"Add configurable context-claim compatibility and migration handling","description":"Implement configuration-driven handling for context-claim naming or mapping so existing deployments can preserve behavior while moving toward the new typed foundation. Include defaults, compatibility mapping, deprecation notes, and rollout behavior. This step should explicitly cover migration semantics rather than claim typing itself.","requires_code":true,"expected_outputs":["Configuration options/defaults for context-claim handling","Compatibility mapping logic","Migration notes for operators","Tests covering old/new config behavior and defaulting"],"success_criteria":["Existing configurations continue to work or fail with clear guidance","Default behavior is well-defined and documented","Compatibility mapping is covered by automated tests","Operators have explicit rollout/migration notes"]},{"name":"Add issuer metadata, JWKS exposure, and stable key identification","description":"Implement or refine issuer metadata and JWKS support needed for downstream token verification, including stable key identifiers (`kid`) and any required discovery metadata. Ensure the resulting metadata aligns with existing patterns and does not break current consumers. This provides a solid verification foundation for newly minted and exchanged tokens.","requires_code":true,"expected_outputs":["Issuer metadata endpoint or metadata updates","JWKS endpoint or JWKS enhancements","Stable key identification strategy","Tests validating metadata/JWKS correctness"],"success_criteria":["Issued tokens can be validated using exposed metadata/JWKS","Key identifiers are present and stable per design","Metadata responses are compatible with existing consumers or documented if additive","Automated tests verify discovery and key material exposure"]},{"name":"Define token-exchange client authentication and authorization model","description":"Specify and implement how clients are authenticated and authorized to use token exchange. Identify trusted caller types, whether public clients are allowed, what credentials/mechanisms are accepted, and how exchange permissions are granted or denied. Reuse existing repository patterns where appropriate, but make the STS-specific trust and permission model explicit before building the exchange endpoint.","requires_code":true,"expected_outputs":["Documented client authn/authz model for token exchange","Authorization checks or policy hooks for exchange callers","Tests covering allowed and denied client scenarios","Configuration or policy definitions for trusted callers"],"success_criteria":["Trusted and untrusted caller categories are explicitly defined","Public-client handling is unambiguous","Exchange permissions are enforced in code and covered by tests","The model aligns with existing repo auth patterns or deviations are documented"]},{"name":"Implement policy contract for issuance and denial handling","description":"Refine the policy interface used by token issuance so project-scoped minting and service identity issuance can rely on a consistent authorization contract. Define clear allow/deny semantics, required inputs, and denial/error paths. Exclude vague optional metadata unless it is concretely required and specified. This step should establish the policy boundary before downstream issuance features consume it.","requires_code":true,"expected_outputs":["Policy interface/contracts for issuance decisions","Denial/error handling behavior","Tests for policy allow/deny cases","Developer notes on policy input/output semantics"],"success_criteria":["Policy decision inputs and outputs are explicit and stable","Denied issuance produces predictable, test-covered behavior","Downstream issuance flows can depend on this interface without custom logic","No unspecified optional metadata remains in the core contract"]},{"name":"Implement project-scoped and service identity token minting on the policy foundation","description":"Build the core issuance paths for project-scoped tokens and service identity tokens using the typed claims, metadata/JWKS, and policy contract established earlier. Make the dependency on the policy layer explicit. Ensure claim sets, audiences, lifetimes, and issuer/signing behavior follow the design and remain within MVP scope.","requires_code":true,"expected_outputs":["Issuance logic for project-scoped tokens","Issuance logic for service identity tokens","Integration tests for successful and denied minting","Examples of resulting token claims"],"success_criteria":["Project and service issuance flows work end-to-end","Issued tokens conform to the claim/profile rules defined earlier","Policy controls are enforced consistently","Integration tests cover happy-path and denial-path scenarios"]},{"name":"Implement RFC 8693 subject-token exchange endpoint","description":"Implement the MVP token exchange endpoint for RFC 8693, centered on subject-token exchange. Support request validation, client authentication/authorization per the defined model, subject token validation, exchange policy enforcement, output token issuance, and RFC-aligned error handling. Keep the initial delivery focused on the required exchange type and avoid speculative extensions unless already approved in scope.","requires_code":true,"expected_outputs":["Token exchange endpoint implementation","Request/response validation and RFC-aligned error mapping","Integration tests for successful exchange and expected failures","Documentation of supported grant parameters and limitations"],"success_criteria":["A compliant subject-token exchange flow works end-to-end","Unauthorized or malformed requests fail with expected errors","Exchange outputs follow the defined token profile and signing rules","Tests cover core RFC 8693 request/response behavior within MVP scope"]},{"name":"Add revocation-aware hooks and token identifiers only","description":"Introduce minimal revocation-aware primitives needed for future hardening, constrained to stable token identifiers such as `jti` and interface/hooks for revocation checks or event integration. Do not implement a new full revocation store unless one already exists and is required. The goal is to avoid blocking future revocation support while keeping MVP scope controlled.","requires_code":true,"expected_outputs":["Stable token identifier support (for example `jti`) where applicable","Revocation/check hook interfaces or extension points","Tests for identifier presence and hook invocation behavior","Short note describing what is intentionally not implemented"],"success_criteria":["Tokens include required identifiers consistently where designed","Hook/interface points exist without expanding into full revocation infrastructure","No unintended persistence/service complexity is introduced","Tests verify the limited revocation-aware behavior implemented"]},{"name":"Validate backward compatibility, migration behavior, tests, and implementation docs","description":"Complete end-to-end validation and implementation-facing documentation. Add compatibility tests for existing token acceptance behavior, config defaulting/migration behavior, and additive API changes. Consolidate examples, operator rollout notes, and developer documentation here rather than in the planning step. Confirm that deferred items remain documented as deferred and do not leak into MVP acceptance criteria.","requires_code":true,"expected_outputs":["Backward compatibility and migration test suite additions","Operator rollout/migration notes","Developer/API usage documentation and examples","Final summary of MVP delivered vs deferred items"],"success_criteria":["Existing supported behavior is preserved or intentional changes are documented","Migration/defaulting behavior is covered by tests","Docs provide enough detail for operators and developers to adopt the changes","Deferred features remain clearly excluded from MVP completion criteria"]}]}
