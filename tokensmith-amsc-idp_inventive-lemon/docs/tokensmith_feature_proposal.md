{"steps":[{"name":"Repository reconnaissance and baseline capture","description":"Inspect the repository to document the current authentication and authorization architecture, token issuance and validation flows, endpoint structure, configuration patterns, signing algorithms, key storage approach, existing client authN/authZ model for mint/exchange-like endpoints, current error response conventions, and all known token consumers. Capture compatibility-sensitive behaviors such as request-body context inputs, multi-project behavior, claim names/semantics, and any existing issuer/JWKS metadata exposure.","requires_code":true,"expected_outputs":["Inventory of auth-related modules, routes, configs, and tests","Baseline summary of current token formats, claims, algorithms, and key handling","List of existing token consumers and compatibility constraints","Summary of current client authentication/authorization and error response conventions"],"success_criteria":["All relevant auth/token components and routes are identified","Current signing, validation, and key management behavior is documented","Compatibility risks for existing consumers are explicitly listed","Current endpoint auth and error conventions are clear enough to drive later design decisions"]},{"name":"Write implementation proposal with explicit standards and migration decisions","description":"Produce the Markdown proposal that defines the intended feature set, architecture, and phased delivery. Make explicit the token taxonomy to be implemented now versus scaffolded or deferred; the RFC 8693 mapping; the client authentication and authorization model for token exchange; the subject token trust/validation model (local JWTs, trusted external JWTs, opaque tokens via introspection, or restricted support); backward compatibility impact on existing token consumers; migration and deprecation plan for claim/schema/context changes; OAuth-compatible error model; issuer metadata/JWKS standards exposure; and any deferred items such as delegated/envelope or proof-bound extensions.","requires_code":false,"expected_outputs":["Markdown implementation proposal","Phased scope table distinguishing implemented, scaffolded, and deferred capabilities","Explicit compatibility and migration section","Explicit token exchange auth, trust, and error-model section"],"success_criteria":["Proposal is internally consistent and aligned with repository baseline","Implemented vs deferred scope is unambiguous","Backward compatibility and migration expectations are explicit","Token exchange client auth, trust model, and OAuth error semantics are specified clearly enough to implement"]},{"name":"Define typed token claim profiles and serialization rules","description":"Design and implement typed claim models for the first-class token profiles in scope, such as access tokens, service tokens, and exchanged tokens, while keeping any deferred profiles clearly marked as scaffolding only if needed. Add configurable context-claim support, define claim serialization/deserialization rules, preserve legacy claim names/semantics where required, and handle time-based claims with explicit clock skew behavior for iat, nbf, and exp. Ensure the models support auditability and policy inputs without forcing premature implementation of deferred token types.","requires_code":true,"expected_outputs":["Typed claim structures and validation logic","Serialization/deserialization compatibility rules","Clock-skew handling for temporal claims","Compatibility notes or adapters for legacy claim formats"],"success_criteria":["First-class token profiles are modeled explicitly and compile/integrate cleanly","Claims round-trip correctly through serialization/deserialization","Legacy-compatible claims remain available where required","Temporal validation behavior is deterministic and covered by tests or clear rules"]},{"name":"Add issuer metadata, JWKS, and key identification support","description":"Implement or refine issuer metadata and JWKS exposure, including appropriate well-known discovery metadata where applicable, key identifiers, algorithm advertisement, and issuer consistency checks. Define JWKS caching and rotation expectations even if full rotation is deferred, and ensure downstream validators can select keys by kid and verify issuer/algorithm constraints consistently.","requires_code":true,"expected_outputs":["Issuer metadata endpoint or configuration","JWKS endpoint/output with kid support","Algorithm and issuer validation rules","Notes on cache and rotation expectations"],"success_criteria":["Metadata and JWKS are exposed in a standards-aligned format","Issued tokens contain enough information for key selection and validation","Issuer and algorithm consistency checks are enforced or documented","Tests or manual validation confirm kid-based verification works"]},{"name":"Implement project-scoped minting and compatibility behavior","description":"Enforce project-scoped minting rules as a business-policy layer while preserving backward compatibility where necessary. Define precedence rules between signed token claims and request fields, support deprecation or transition behavior for callers that currently send context in request bodies or depend on multi-project behavior, and make the migration path explicit for existing clients.","requires_code":true,"expected_outputs":["Project-scoped minting logic","Precedence rules for token claims vs request parameters","Transition/deprecation behavior for legacy callers","Updated configuration or feature flags if needed"],"success_criteria":["Minting behavior follows project-scoped policy consistently","Legacy request patterns have defined handling rather than ambiguous failures","Precedence behavior is deterministic and documented","Tests cover both new and legacy-compatible paths"]},{"name":"Introduce explicit policy evaluation boundary","description":"Create a clear policy-evaluation boundary for token issuance and exchange decisions so business rules are isolated from transport and token-format code. Ensure policy inputs include client identity, requested token type, requested audience/resource, scope reduction or expansion rules, project context, and delegation/actor context where relevant. The boundary should support auditability and make future rule changes easier to validate.","requires_code":true,"expected_outputs":["Policy evaluation interface/service","Defined policy input schema","Decision outputs including allow/deny and rationale fields where appropriate","Integration points from mint/exchange handlers into policy layer"],"success_criteria":["Transport handlers no longer embed ad hoc policy decisions","All required policy inputs are present and validated","Policy results are consumable by minting and exchange flows","Audit-relevant decision context is preserved"]},{"name":"Refine service identity token issuance semantics","description":"Implement or update service identity token issuance with explicit subject and identity conventions. Define whether service identity is represented via sub, client_id, a separate claim, or a combination, and ensure downstream authorization and audit trails remain consistent. Align this with typed claims, policy evaluation, and existing token consumers.","requires_code":true,"expected_outputs":["Service identity issuance logic","Documented subject/client identity conventions","Updated claim mappings for service-issued tokens","Tests for downstream-consistent identity semantics"],"success_criteria":["Service tokens carry identity in a consistent and documented manner","Downstream consumers can resolve subject identity unambiguously","Audit and authorization semantics match the documented convention","No regressions appear in existing service-token use cases"]},{"name":"Add lightweight extension points for proof-bound or sender-constrained tokens","description":"Add only minimal extensibility hooks needed to avoid future redesign, such as claim-model placeholders, interfaces, or configuration points for sender-constrained or proof-bound token support. Do not implement a full proof-of-possession feature unless directly required; document the capability as deferred if not in current scope.","requires_code":true,"expected_outputs":["Minimal extension interfaces or claim placeholders","Documentation of deferred proof-bound support","Non-invasive integration points in token models/services"],"success_criteria":["Current implementation is not blocked from future extension","No large new implementation track is introduced","Deferred scope is clearly documented to prevent overbuild"]},{"name":"Implement RFC 8693-aligned token exchange endpoint and service","description":"Build the token exchange endpoint and service in line with RFC 8693 as far as the product scope allows. Explicitly implement client authentication requirements for the token endpoint, authorization rules controlling which clients may exchange which subject tokens for which target audiences/resources/scopes, subject token validation according to the chosen trust model, and output token issuance according to configured format support. Add standards-aligned request parsing, actor/delegation handling only if in scope, and OAuth-compatible error responses such as invalid_request, invalid_grant, invalid_target, unauthorized_client, and unsupported_token_type where applicable.","requires_code":true,"expected_outputs":["Token exchange endpoint and service logic","Client authentication and authorization enforcement for exchange","Subject token validation pipeline for allowed token sources/types","OAuth/RFC-appropriate error responses and mappings","Configuration for supported token formats and exchange options"],"success_criteria":["Authorized clients can perform supported exchanges successfully","Unsupported or invalid exchange requests return standards-compliant error responses","Subject tokens are validated according to the declared trust model","Exchanged token format and claims match the documented design","Tests cover success paths and major failure modes"]},{"name":"Defer or tightly scope delegated/envelope token support","description":"Assess whether delegated or envelope token support is explicitly required for this delivery. If not required, record it as deferred follow-up work with rationale and design notes only. If required, implement only the minimum scaffold necessary and clearly explain why RFC 8693 exchange alone is insufficient for the use case. Avoid introducing a second broad token architecture without a concrete requirement.","requires_code":false,"expected_outputs":["Decision record marking delegated/envelope support as deferred or minimally scoped","If needed, narrow design notes or scaffolding only","Rationale linking the decision to actual requirements"],"success_criteria":["Scope creep is controlled and visible to reviewers","Any delegated/envelope work has a concrete, justified use case","No unnecessary parallel token model is fully implemented"]},{"name":"Add revocation primitives and structured audit logging","description":"Implement revocation-related primitives or hooks appropriate to the current architecture, along with structured audit logging for minting and exchange actions. Distinguish clearly between hooks/primitives and a full revocation system if full revocation is not in scope. Include replay-detection considerations where relevant, and define retention/privacy expectations for audit records.","requires_code":true,"expected_outputs":["Revocation hooks or primitives","Structured audit log events for mint/exchange/policy outcomes","Documentation of replay-detection considerations","Retention/privacy guidance for logs"],"success_criteria":["Security-relevant actions emit structured, useful audit events","Revocation support level is explicit and not overstated","Audit output includes enough context for traceability without violating privacy constraints","Relevant tests or manual checks confirm event emission"]},{"name":"Expand tests, interoperability coverage, and feature documentation","description":"Add comprehensive tests and user/developer documentation for the implemented features. Cover typed claims, project-scoped minting, policy boundaries, service identity issuance, issuer/JWKS validation, kid selection, issuer mismatch, token exchange success and failure cases, OAuth error responses, subject token trust scenarios, and legacy consumer compatibility where possible. Provide configuration examples and operational guidance for deployment and migration.","requires_code":true,"expected_outputs":["Unit/integration/interoperability tests","Examples for configuration and API usage","Feature documentation for operators and developers","Compatibility and migration guidance"],"success_criteria":["Critical paths and failure modes are covered by automated tests","Interoperability cases like JWKS validation and issuer mismatch are tested","Docs are sufficient for configuration, rollout, and troubleshooting","Legacy compatibility expectations are documented and verified where feasible"]},{"name":"Run end-to-end validation and prepare final execution summary","description":"Execute validation across the implemented work, reconcile any deviations from the proposal, and produce a final summary for reviewers. Include what was implemented, what was scaffolded, what was deferred, compatibility impacts, standards deviations if any, outstanding risks, and any required PR notes or changelog entries.","requires_code":true,"expected_outputs":["Validation results summary","List of deviations from proposal and their rationale","Final implementation summary for reviewers","PR notes or changelog updates if required"],"success_criteria":["Implemented behavior is validated against the approved proposal","Any deviations are documented with clear rationale","Reviewer-facing summary is concise, accurate, and complete","Release/PR documentation requirements are satisfied"]}]}
