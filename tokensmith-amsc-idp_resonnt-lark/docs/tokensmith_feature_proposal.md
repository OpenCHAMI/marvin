{"steps":[{"name":"Inspect repository and capture baseline auth/token architecture","description":"Review the repository structure, existing authentication and authorization flows, token issuance and validation logic, configuration mechanisms, API routes, signing key handling, and current test/documentation coverage. Identify whether current tokens are symmetric or asymmetric, how claims are modeled today, whether there is already issuer metadata or JWKS support, and what domain models already exist for users, projects, groups, roles, service identities, or policy inputs. Record constraints, extension points, and compatibility requirements before proposing changes.","requires_code":false,"expected_outputs":["Inventory of relevant modules, endpoints, configs, and tests","Baseline summary of current token formats, signing modes, and validation behavior","List of existing domain attributes that can legitimately feed mint/exchange policy","Risk list covering compatibility, missing primitives, and likely refactor points"],"success_criteria":["All token-related code paths and public API surfaces are identified","Existing signing algorithm/key mode compatibility is understood","Available policy-relevant domain attributes are documented without inventing new ones","Known gaps and implementation constraints are clearly captured"]},{"name":"Write scoped implementation proposal with phased delivery","description":"Produce a Markdown proposal that defines the target scope, phased implementation order, compatibility guarantees, and explicit deferrals. The proposal should separate mandatory work from optional extensions, making clear that the core scope includes typed token modeling, metadata/JWKS exposure, project/service issuance, policy interfaces, config/error compatibility, and RFC 8693 exchange. Sender-constrained or delegated/envelope token ideas should be documented only as future extension points unless they are already required by the repository context. The proposal should also specify whether service identity issuance is a new endpoint, a mode of an existing mint endpoint, or another existing flow extension.","requires_code":false,"expected_outputs":["Markdown design/implementation proposal checked into the repo or prepared for review","Phased scope definition with core deliverables and deferred items","Decision record for service token delivery path and exchange caller trust boundary"],"success_criteria":["Proposal explicitly distinguishes required work from deferred extensions","RFC 8693 work is split into endpoint/contract and policy/issuance phases","Service token path and policy assumptions are unambiguous","Reviewers can trace each implementation step back to a concrete design decision"]},{"name":"Define typed token models and issuance refactor boundaries","description":"Introduce or refine typed internal models for the mandatory token profiles needed in this pass, such as user access tokens, project-scoped tokens, and service identity tokens. Define claim schemas, validation rules, and serialization boundaries so issuance and exchange logic operate on typed structures rather than ad hoc claim maps where feasible. Add configurable context-claim support only for claims that can be cleanly derived from existing models and config. Do not implement optional delegated/envelope token classes unless they are confirmed requirements.","requires_code":true,"expected_outputs":["Typed token profile/model definitions","Validation rules for required claims and token variants","Refactored issuance interfaces or helpers aligned to typed models","Documentation/comments describing mandatory versus deferred token profiles"],"success_criteria":["Mandatory token profiles are explicitly defined and compile/integrate cleanly","Issuance code uses typed models at key boundaries","Optional token types are not ambiguously half-implemented","Claim validation failures are deterministic and testable"]},{"name":"Add configuration migration, defaults, and validation safeguards","description":"Introduce any required new configuration fields for issuer metadata, JWKS exposure, token profile behavior, exchange settings, policy hooks, and service/project minting, while preserving backward-compatible defaults where intended. Validate invalid combinations early at startup or config load time. Ensure parity across supported configuration sources such as files, environment variables, or CLI flags if the repository uses them. Document migration impacts and any operational constraints such as claim-size or request-size limits, even if enforcement is only partially implemented in this pass.","requires_code":true,"expected_outputs":["Updated config schema/parsing/defaults","Startup or load-time validation for invalid config combinations","Migration notes and deployment guidance","Documented operational limits or explicit deferrals"],"success_criteria":["Existing deployments preserve old behavior unless new features are enabled","Invalid config combinations are rejected with clear errors","New config is reachable through all supported config mechanisms","Migration notes are sufficient for operators to adopt the change safely"]},{"name":"Standardize API error model and compatibility behavior","description":"Define and implement consistent error handling across token minting, token exchange, metadata discovery, JWKS retrieval, validation failures, policy denials, unsupported token types, and malformed requests. Align response codes and payloads with existing repository conventions and relevant standards where applicable. Ensure the error model is documented and tested so new functionality does not introduce inconsistent API behavior.","requires_code":true,"expected_outputs":["Error taxonomy and response mapping","Updated handlers/middleware/utilities for consistent error generation","Documentation of error responses for core auth/token endpoints"],"success_criteria":["Equivalent classes of failures return consistent status codes and response shapes","Policy denial, validation failure, and unsupported-operation cases are distinguishable","New endpoints match established API conventions","Tests verify representative error paths"]},{"name":"Expose issuer metadata, JWKS, and key identifier compatibility","description":"Implement or complete standards-aligned issuer metadata and JWKS endpoints, including stable issuer values, key identifiers, and any necessary signing algorithm disclosures. Preserve compatibility with existing signing modes, whether symmetric-only, asymmetric, or mixed, and document any limitations if JWKS exposure only applies to asymmetric verification keys. Ensure token headers and validators consistently use key identifiers where appropriate.","requires_code":true,"expected_outputs":["Issuer metadata endpoint or equivalent discovery surface","JWKS endpoint or documented compatibility behavior","Consistent kid handling in token issuance/validation","Tests covering metadata and key discovery behavior"],"success_criteria":["Metadata/JWKS endpoints respond correctly and match actual signing configuration","kid values are stable and validated end-to-end where applicable","Existing token validation behavior remains compatible","Asymmetric/symmetric compatibility constraints are documented and enforced"]},{"name":"Implement issuance-model updates for project-scoped and service identity tokens","description":"Build the core issuance changes for project-scoped tokens and service identity tokens using the typed models and config introduced earlier. Clarify and implement the selected service-token path: either an extension of an existing mint endpoint, a new route, or a policy-controlled mode. Project-scoped issuance should narrow claims and permissions to the requested project according to existing domain relationships and configured rules. Service issuance should use explicit service identity policy checks and produce auditable token claims distinct from end-user tokens.","requires_code":true,"expected_outputs":["Updated issuance logic for project-scoped tokens","Service identity token issuance flow","Claim-shaping rules for user/project/service token variants","Tests covering successful issuance and denial cases"],"success_criteria":["Project-scoped tokens contain only approved project context and scopes","Service tokens are issued through a clearly defined and documented path","Behavior reuses existing domain models and does not invent unsupported attributes","Backward compatibility is preserved for unchanged mint flows"]},{"name":"Add explicit policy evaluation interfaces for minting and exchange","description":"Create clear policy interfaces or service boundaries for authorization decisions during minting and token exchange. Policy inputs should be derived only from existing repository domain models and caller identity, such as authenticated client, user identity, project membership, existing role mappings, or service identity metadata if present. Unknown or not-yet-modeled attributes should be deferred, not invented. Ensure policy decisions can support narrowing, denial, subject restrictions, and service/project-specific authorization while remaining testable and auditable.","requires_code":true,"expected_outputs":["Policy interface definitions and implementations/adapters","Documented policy input schema derived from existing models","Audit-friendly decision outputs or denial reasons","Unit tests for core allow/deny/narrowing scenarios"],"success_criteria":["Policy logic is isolated from transport and token serialization details","All policy inputs come from real existing models or authenticated caller context","Allow/deny outcomes are reproducible in tests","Minting and exchange code consume the same policy boundary consistently"]},{"name":"Implement RFC 8693 endpoint contract, request validation, and caller authentication","description":"Add the token exchange endpoint surface aligned to RFC 8693 for the supported subset in this pass. Implement request parsing, authentication of the exchanging client according to existing repository patterns, validation of required and supported parameters, subject token parsing, requested token type handling for the supported variants, and standards-aligned error responses. Clearly define how caller identity and authentication results are conveyed into exchange policy evaluation.","requires_code":true,"expected_outputs":["Token exchange endpoint/route","Request/response contract for supported RFC 8693 parameters","Caller authentication integration for exchange requests","Validation and error-path tests for malformed/unsupported requests"],"success_criteria":["Exchange caller must be authenticated/authorized according to repository conventions","Unsupported or malformed requests fail with consistent, documented errors","Supported request parameters are validated before issuance logic runs","Caller identity is available to downstream policy evaluation in a defined form"]},{"name":"Implement RFC 8693 exchange policy, token issuance, and narrowing behavior","description":"Build the exchange service behavior behind the validated endpoint: validate the incoming subject token, apply exchange authorization policy, enforce allowed subject and audience transformations, issue the exchanged token using the typed token models, and support the minimum required RFC 8693 semantics for this repository. Keep the feature set intentionally scoped to what is feasible now, such as required parameters and supported token types, while documenting unsupported actor/delegation or advanced semantics as deferred unless already required. Include claim narrowing, project/service targeting where allowed, and audit logging of exchange decisions.","requires_code":true,"expected_outputs":["Exchange service implementation","Issued exchanged-token claim mapping and narrowing rules","Audit logs for exchange requests and policy decisions","Behavioral tests for successful exchange and denial scenarios"],"success_criteria":["Valid subject tokens can be exchanged only under explicit policy authorization","Exchanged tokens reflect requested/allowed narrowing without privilege escalation","Unsupported advanced RFC 8693 semantics are clearly rejected or documented as deferred","Audit records capture requester, subject, target context, and decision outcome"]},{"name":"Add revocation primitives, audit logging, and operational safeguards","description":"Implement or extend revocation-related primitives appropriate to the repository architecture, such as jti tracking, deny lists, token family invalidation hooks, or short-lived token compatibility. Add structured audit logging for mint, exchange, denial, and revocation-relevant events. Where feasible, enforce basic operational safeguards such as request-size, claim-size, or rate-limiting hooks for exchange-sensitive paths; otherwise document them explicitly as operational requirements or future work.","requires_code":true,"expected_outputs":["Revocation-related storage/hooks/primitives","Structured audit log events for token operations","Basic safeguard enforcement or documented hook points","Tests for revocation/audit behavior where practical"],"success_criteria":["Security-relevant token operations generate structured audit events","Revocation primitives integrate with current validation/issuance architecture","Operational safeguards are either implemented or clearly documented as deferred constraints","No silent security-sensitive failure paths remain"]},{"name":"Expand tests, examples, and user/operator documentation","description":"Add comprehensive automated coverage for typed claims, config validation, metadata/JWKS, project/service issuance, policy boundaries, exchange endpoint validation, exchange authorization, error responses, revocation hooks, and backward compatibility. Update examples, API docs, configuration docs, and migration guidance so developers and operators can adopt the new functionality. Include explicit notes on deferred features such as sender-constrained or delegated/envelope tokens if they were not implemented.","requires_code":true,"expected_outputs":["Unit/integration tests for all implemented features","Updated API and configuration documentation","Examples or sample requests for minting and exchange flows","Migration and compatibility notes"],"success_criteria":["Automated tests cover core success and failure paths for all new features","Documentation matches actual implementation behavior","Backward-compatibility expectations are explicitly described","Deferred capabilities are clearly identified to avoid scope confusion"]},{"name":"Produce final verification summary and execution artifact","description":"Summarize what was implemented, what was deferred, how compatibility was preserved, what tests were run, and any remaining risks or follow-up recommendations. Include a concise mapping from the approved proposal to completed code changes and note any deviations encountered during implementation. This provides reviewers with a final audit trail of delivery against scope.","requires_code":false,"expected_outputs":["Final implementation summary","Verification checklist with completed tests and validations","List of deferred items and rationale","Reviewer-ready change map from plan to implementation"],"success_criteria":["Implemented versus deferred scope is explicit and accurate","Verification evidence is complete and easy to review","Any deviations from the original proposal are documented with reasons","Reviewers can assess delivery readiness without re-deriving project history"]}]}
