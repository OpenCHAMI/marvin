{"steps":[{"name":"Repository inspection and baseline inventory","description":"Inspect the repository to identify the current token issuance, signing, verification, routing, configuration, and authentication patterns. Explicitly inventory protected endpoint authentication, current client/service authentication methods, claim construction/parsing logic, any existing token profile abstractions, signing algorithm and key management approach, issuer/audience handling, and whether persistence exists for revocation, audit, or policy inputs. Capture constraints that affect RFC 8693 token exchange, JWKS exposure, and backward compatibility.","requires_code":false,"expected_outputs":["Baseline architecture notes","Inventory of token-related packages, endpoints, configs, and auth flows","List of existing signing/key capabilities and gaps","List of persistence and observability primitives relevant to policy/audit/revocation"],"success_criteria":["All token issuance/validation entry points are identified","Current client/service authentication approach is documented","Signing mode (symmetric vs asymmetric) and key exposure feasibility are clear","Unknowns and implementation risks are reduced to a short explicit list"]},{"name":"Write implementation proposal with migration and compatibility matrix","description":"Produce a Markdown proposal describing the implementation approach, sequencing, endpoint and model changes, standards alignment, and rollout considerations. Include a dedicated migration/compatibility matrix covering existing endpoint to new endpoint mapping, old claim names to configured claim names, unchanged vs changed behaviors, and whether legacy tokens remain accepted. Also document any feature flags, config gates, or phased rollout assumptions needed to avoid breaking existing clients.","requires_code":false,"expected_outputs":["Markdown implementation proposal","Migration/compatibility matrix","List of breaking vs non-breaking changes","Rollout and feature-gating notes"],"success_criteria":["Proposal covers all required features and dependencies","Compatibility behavior is explicit for endpoints, claims, and token parsing","Reviewers can identify rollout risk and mitigation from the document alone","Open design decisions are few and clearly labeled"]},{"name":"Define typed token profiles and configurable claim foundations","description":"Introduce typed models for token profiles and claim construction, making explicit whether types are internal-only, serialized JWT claim structs, or both. Add configurable context-claim naming and a disciplined mapping layer so claim emission and parsing do not drift into ad hoc maps. Include compatibility handling for legacy token parsing if existing tokens must remain valid. Ensure these foundations are reusable by minting, service identity issuance, and token exchange paths.","requires_code":true,"expected_outputs":["Typed token/profile models","Claim mapping and configuration primitives","Legacy parsing compatibility layer if needed","Developer notes on internal vs external claim typing boundaries"],"success_criteria":["Core token types can be constructed and parsed through typed APIs","Configured claim names are applied consistently","Legacy tokens still parse/validate when compatibility is required","Map-based claim handling is limited to controlled extension points"]},{"name":"Add explicit policy decision interfaces","description":"Define policy interfaces used by minting and exchange flows before implementing endpoint-specific authorization logic. The contract should include decision outcome, denial reason/code, and an evaluated attributes summary suitable for audit logging. Identify the minimum request attributes, subject attributes, project/context attributes, and client attributes that policy evaluation requires, while keeping the implementation simple enough to stub or wire into existing authorization mechanisms.","requires_code":true,"expected_outputs":["Policy decision interfaces and types","Initial policy evaluator or adapter implementation","Error/denial mapping rules","Audit-oriented decision metadata schema"],"success_criteria":["Minting and exchange code can depend on a stable policy contract","Policy decisions include outcome, reason, and evaluated attribute summary","Unauthorized vs invalid requests can be distinguished consistently","Interfaces are reusable without embedding endpoint-specific logic"]},{"name":"Expose issuer metadata, JWKS, and key identifier behavior","description":"Implement issuer metadata and JWKS exposure needed for interoperable verification, including stable key identifiers. If the current system uses symmetric signing, explicitly document either a migration path to asymmetric signing or a scoped limitation for this pass. Define cache headers or stability expectations for JWKS responses and clarify whether key rotation is implemented now or deferred. Ensure metadata reflects actual token issuer behavior and supported signing algorithms.","requires_code":true,"expected_outputs":["Issuer metadata endpoint(s)","JWKS endpoint","kid assignment/selection behavior","Notes on symmetric-to-asymmetric migration or scoping, if applicable"],"success_criteria":["Published metadata matches actual issuer and signing configuration","JWKS can be consumed by standard JWT verification tooling","kid values are present and stable enough for verification use","Caching/rotation expectations are documented and testable"]},{"name":"Implement project-scoped minting semantics","description":"Implement the project-scoped token minting flow with a clear separation between request validation and authorization semantics. Enforce request-shape rules such as exactly one allowed context where required, validate input claims and scopes, and ensure downstream authorization relies on signed token claims rather than trusting mutable request bodies after issuance. Wire policy checks through the explicit policy interface and emit typed claims using the new token profile layer.","requires_code":true,"expected_outputs":["Updated minting service/endpoint behavior","Validation rules for project/context inputs","Policy-integrated authorization checks","Issued project-scoped tokens using typed claims"],"success_criteria":["Invalid requests fail early with consistent errors","Authorization decisions are made through policy interfaces","Issued tokens contain correct project/context claims","Downstream trust model is based on signed token content, not request echoing"]},{"name":"Implement or refine service identity token issuance","description":"Add or refine issuance for service identity tokens using the typed token/profile layer and policy interfaces. Explicitly document whether service authentication already exists and is reused, or whether this step only assumes an authenticated service principal from existing middleware. Ensure service identity claims, audiences, and scopes are well defined and compatible with later exchange behavior.","requires_code":true,"expected_outputs":["Service identity issuance logic","Documented service principal assumptions or integration points","Service token claim definitions","Tests for successful and denied service issuance"],"success_criteria":["Authenticated services can obtain correctly formed service tokens","Service token claims are distinct from user/project tokens where needed","Authorization for service issuance is policy-controlled","Scope/audience semantics are documented and consistent"]},{"name":"Implement RFC 8693-aligned token exchange with explicit client authentication and authorization","description":"Implement the token exchange endpoint and service aligned to RFC 8693, including explicit client authentication and authorization requirements. Reuse or define the supported client authentication methods for the exchange endpoint, map authenticated clients to allowed exchange operations, and return distinct errors for unauthenticated vs authenticated-but-unauthorized callers. Validate subject tokens, requested token type, audience/resource/scope semantics, and define precedence rules when audience, resource, and scope requests conflict. Also clarify and implement how impersonation vs delegation are represented, and whether the exchanged token preserves the original subject or introduces actor/delegation claims.","requires_code":true,"expected_outputs":["Token exchange endpoint/service","Client authentication integration for exchange","Authorization rules mapping clients to allowed exchanges","Claim semantics for exchanged tokens","Error mapping for RFC 8693-related failures"],"success_criteria":["Exchange requests require authenticated clients","Authenticated clients can only perform authorized exchanges","Subject tokens are validated for issuer, audience, time validity, and type","Conflict handling for audience/resource/scope is deterministic","Issued exchanged tokens reflect defined subject/delegation semantics"]},{"name":"Add structured audit logging and minimal revocation hooks","description":"Implement required structured audit logging for minting and exchange decisions, including policy outcome, denial reason, evaluated attribute summary, token type, client identity, and correlation metadata. Add only minimal revocation primitives or integration hooks appropriate to the repository’s architecture, such as denylist/status-check interfaces or extension points, without committing to a full persistence-backed revocation system unless such infrastructure already exists. Keep logging mandatory in this pass and revocation intentionally narrow.","requires_code":true,"expected_outputs":["Structured audit log events for minting/exchange","Logging field schema/documentation","Revocation/status-check interfaces or hooks","Integration notes describing current revocation limitations"],"success_criteria":["All minting and exchange paths emit structured audit events","Logs include enough information to trace policy decisions and failures","Revocation extension points exist without overpromising unsupported behavior","No full revocation implementation is attempted unless existing infrastructure supports it"]},{"name":"Add sender-constrained token extension scaffolding","description":"Introduce scaffold-only extension points for sender-constrained or proof-bound tokens. Limit this step to interfaces, model placeholders, and claim/header extension hooks so future support can be added cleanly. Do not implement production sender-constrained behavior in this pass unless it is already trivial within existing architecture. Explicitly mark unsupported behaviors and avoid changing current issuance semantics beyond the necessary extensibility points.","requires_code":true,"expected_outputs":["Extension interfaces/placeholders for proof-bound semantics","Minimal model or claim/header hooks","Documentation marking support as deferred/scaffold-only"],"success_criteria":["Core token flows remain unchanged unless extension points are invoked","Future sender-constrained support can be added without major refactors","No accidental partial production support is exposed","Deferred scope is explicit in code and docs"]},{"name":"Add delegated/envelope token scaffolding or design note","description":"Provide only a narrow scaffold or explicit design note for delegated/envelope token support, unless the repository already has a very clean compatible structure. Avoid introducing a parallel fully implemented token profile before token exchange is stable. If any code is added, keep it limited to interfaces or model placeholders that do not complicate current minting and exchange behavior.","requires_code":true,"expected_outputs":["Delegated/envelope support design note and/or minimal interfaces","Documented rationale for deferral or narrow scope","Optional placeholder models if architecture benefits from them"],"success_criteria":["No major implementation effort is spent on non-core delegated/envelope behavior","Current token model remains coherent and stable","Future delegated/envelope work has a documented entry point","Scope remains aligned with core deliverables"]},{"name":"Expand tests, examples, documentation, and execution summary","description":"Add comprehensive tests for issuance, metadata, JWKS, policy behavior, service identity issuance, and token exchange. Include positive and negative cases such as malformed JWTs, wrong issuer/audience, expired or not-yet-valid subject tokens, unknown kid, unsupported token types, unauthorized exchange clients, and attempts to broaden privileges through exchange. Update examples and documentation to show configuration, endpoint usage, compatibility behavior, and deferred features. Produce a concise execution summary of what was implemented versus deferred.","requires_code":true,"expected_outputs":["Unit/integration test suite updates","Example requests/responses and config snippets","Updated docs/proposal notes","Execution summary with implemented vs deferred items"],"success_criteria":["Tests cover all core success and failure paths","Negative cases include malformed, invalid, expired, unauthorized, and privilege-broadening scenarios","Documentation matches actual behavior and configuration","Implemented vs deferred scope is transparent to reviewers"]},{"name":"Compatibility and rollout validation","description":"Perform a dedicated end-to-end compatibility and rollout validation pass. Verify whether legacy tokens remain accepted as intended, whether old and new claim names are handled per the migration matrix, whether existing endpoints remain supported or redirect cleanly, and whether feature flags/config gates behave correctly across rollout stages. Confirm that no undocumented breaking changes were introduced and that client authentication for token exchange interoperates with existing deployment assumptions.","requires_code":true,"expected_outputs":["Compatibility validation checklist/results","Rollout test results for feature-gated behavior","Confirmed migration notes or final adjustments","List of any remaining breaking changes requiring explicit approval"],"success_criteria":["Migration matrix behavior is verified in running code","Legacy compatibility works where promised","Feature-gated rollout path is testable and documented","Any remaining breaking changes are explicit, minimal, and approved"]}]}
