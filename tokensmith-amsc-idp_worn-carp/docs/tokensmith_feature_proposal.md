{"steps":[{"name":"Repository reconnaissance and trust-boundary inventory","description":"Inspect the repository to identify current token minting, validation, authn/authz boundaries, HTTP routing, configuration, key management, and test infrastructure. Explicitly map where client authentication happens today, where authorization decisions are made for issuance and exchange, what token formats and claim names already exist, and what subject-token trust assumptions are currently supported or possible (local issuer only, trusted external issuers, introspection hooks, trust stores). Capture legacy compatibility constraints that later design and rollout must preserve.","requires_code":false,"expected_outputs":["Architecture inventory of token, HTTP, config, and key-management components","List of existing authn/authz boundaries for minting and exchange-related flows","Catalog of current token formats, claims, and compatibility constraints","Initial assessment of feasible subject-token trust models and client authentication methods"],"success_criteria":["All components needed for minting, validation, metadata, and exchange are identified","Authn/authz decision points are explicitly documented","Legacy token and caller compatibility risks are listed","Feasible trust-boundary options for subject-token validation are clear enough to drive design choices"]},{"name":"Write and refine the scoped design proposal","description":"Produce a Markdown design proposal that reflects the revised scope and explicitly separates: features in scope for this implementation pass, scaffolding-only work, and deferred items. The proposal should define supported exchange client authentication methods, whether public clients are allowed, how client identity feeds policy evaluation, and the trust boundary for subject-token validation in the first pass. It should also spell out compatibility and migration expectations, including legacy claims, wire-format impact, rollout order for issuer/JWKS changes, and any backward-compatible defaults. Include standards alignment notes for issuer metadata, JWKS, OAuth/RFC 8693 handling, and error semantics.","requires_code":false,"expected_outputs":["Markdown proposal document","Explicit in-scope vs scaffolding vs deferred feature list","Documented first-pass decisions for client auth, trust boundaries, compatibility, and migration"],"success_criteria":["Proposal removes ambiguity around optional features and deferred work","Client authentication and subject-token trust decisions are explicitly documented","Compatibility and migration behavior is clearly specified","Proposal is specific enough to guide implementation without major scope uncertainty"]},{"name":"Implement core typed claims and minimal token profile foundations","description":"Introduce typed claim structures and configuration plumbing only for the token profiles required by the first implementation pass. Preserve existing wire compatibility where needed by supporting legacy claim names or adapters as defined in the proposal. Avoid broad abstractions for hypothetical token profiles; instead, create narrowly scoped foundations that support service identity issuance, project-scoped minting, and token exchange outputs. Ensure serialization, validation, and config defaults are stable and testable.","requires_code":true,"expected_outputs":["Typed claim/profile definitions for in-scope token flows","Serialization and validation logic for required claim sets","Compatibility adapters or fallback handling for legacy claims where needed"],"success_criteria":["Only in-scope token profiles are introduced","New claim handling supports required issuance and exchange flows","Legacy compatibility behavior matches the design proposal","Tests or validation checks confirm stable serialization and parsing"]},{"name":"Expose standards-compatible issuer metadata, JWKS, and key identifiers","description":"Add or refine endpoints and internal support for issuer metadata and JWKS publication with standards-compatible response shapes. Ensure signing keys expose stable key identifiers, issuer values are canonical, and metadata is sufficient for downstream JWT validation in exchange scenarios. Include any required configuration and documentation for rollover-safe behavior.","requires_code":true,"expected_outputs":["Issuer metadata endpoint or equivalent response","JWKS endpoint or equivalent response","Stable key identifier support and related configuration"],"success_criteria":["Metadata and JWKS responses match expected standards-compatible shapes","Issued JWTs reference resolvable key identifiers","Configuration supports stable issuer/key publication behavior","Validation tests confirm consumers can discover issuer and keys correctly"]},{"name":"Add project-scoped minting and backward-compatible request validation","description":"Implement request validation and minting changes so tokens can be issued with project or context scoping where required. Define and enforce when context is mandatory, and preserve backward compatibility for legacy callers that omit context in flows where context remains optional. Ensure validation errors are explicit and integrate with policy evaluation inputs.","requires_code":true,"expected_outputs":["Project/context-aware minting logic","Request validation rules for required and optional context","Backward-compatible behavior for legacy callers when context is not required"],"success_criteria":["Scoped issuance works for intended callers and flows","Legacy callers continue to work where allowed by design","Invalid or missing required context is rejected consistently","Tests cover both scoped and backward-compatible legacy cases"]},{"name":"Introduce policy-aware minting and exchange decision interfaces","description":"Implement policy evaluation hooks for minting and exchange with deny-by-default behavior. Clearly separate minimum required policy inputs from optional enrichments so decisions are predictable and testable. Ensure policy inputs include authenticated client identity, requested audience/resource/scope, subject-token properties, project/context information, and any other first-pass required attributes. Document default-deny semantics and fallback behavior when optional enrichments are unavailable.","requires_code":true,"expected_outputs":["Policy evaluation interfaces for minting and exchange","Defined minimum required policy input set","Deny-by-default enforcement in issuance/exchange paths"],"success_criteria":["All sensitive issuance and exchange decisions pass through policy hooks","Required policy inputs are always present or requests fail deterministically","Optional enrichments do not create ambiguous authorization behavior","Tests demonstrate deny-by-default and allowed-path outcomes"]},{"name":"Implement service identity token issuance","description":"Add issuance support for service identity tokens using the new typed claims, policy checks, issuer metadata, and configuration foundations. Ensure issued tokens have the expected subject/audience/lifetime semantics and remain compatible with downstream validation logic. Integrate project scoping where applicable.","requires_code":true,"expected_outputs":["Service identity token issuance path","Configuration for service-token issuance behavior","Tests for successful and rejected issuance scenarios"],"success_criteria":["Service identity tokens can be minted end-to-end","Issued tokens contain the expected claims and signatures","Policy and project-scoping constraints are enforced","Automated tests verify issuance behavior and validation compatibility"]},{"name":"Implement RFC 8693-aligned token exchange endpoint","description":"Build the token exchange endpoint and core STS flow aligned with RFC 8693 for the scoped first pass. Explicitly enforce the chosen client authentication methods, determine whether public clients are rejected, and feed authenticated client identity into policy evaluation. Define and implement the first-pass subject-token trust boundary: for example, locally issued JWT subject tokens only, or a limited configurable set of trusted issuers if approved in the design. Support the chosen grant parameters, validate subject tokens, apply policy, mint exchanged tokens, and return standards-compliant OAuth responses. Define explicit OAuth error codes, HTTP status mappings, machine-readable error bodies, and correlation/logging behavior.","requires_code":true,"expected_outputs":["Token exchange HTTP endpoint","Subject-token validation logic for first-pass trust model","Client authentication and authorization enforcement for exchange requests","Standards-compliant success and error responses"],"success_criteria":["Endpoint successfully processes supported RFC 8693 exchange requests","Unsupported client auth modes or trust models fail as designed","Subject-token validation matches the documented trust boundary","Responses use the documented OAuth error codes, status mappings, and response shapes","End-to-end tests cover valid exchanges and representative failure cases"]},{"name":"Add extension scaffolding for future token profiles only if lightweight","description":"If needed and low-cost, add narrowly documented extension points for future token profiles such as delegated/envelope or sender-constrained variants, without implementing full behavior unless explicitly required. This step should be limited to interfaces, configuration placeholders, or type-extension seams that reduce future rework and must not expand the first-pass deliverable. If such scaffolding is not clearly beneficial, defer it entirely and document future work instead.","requires_code":true,"expected_outputs":["Optional minimal extension interfaces or placeholders","Documentation of deferred token-profile extensions and integration points"],"success_criteria":["No unnecessary full implementation of deferred token profiles is introduced","Any scaffolding added is small, isolated, and non-invasive","Deferred work is clearly documented without affecting current functionality"]},{"name":"Add revocation hooks and structured audit logging primitives","description":"Implement revocation-related primitives or hooks and structured audit logging for issuance and exchange events. Be explicit that this step provides hooks/primitives unless full revocation semantics are already supported elsewhere. Ensure logs capture key security-relevant fields, decision outcomes, and correlation identifiers while avoiding sensitive data leakage.","requires_code":true,"expected_outputs":["Revocation hook interfaces or primitives","Structured audit log events for minting and exchange","Correlation identifiers integrated into security-relevant logs"],"success_criteria":["Security-relevant issuance and exchange actions emit structured logs","Revocation support is accurately scoped as hooks/primitives or full support, per implementation","Logs contain enough context for auditing without exposing prohibited sensitive material","Tests or inspection confirm correlation behavior and event coverage"]},{"name":"Expand automated tests, examples, and configuration/error documentation","description":"Add comprehensive automated tests for typed claims, metadata/JWKS, project-scoped minting, service issuance, policy denial, exchange success/failure paths, compatibility behavior, and error semantics. Update examples and configuration docs to show supported client authentication, trust boundaries, migration expectations, and standards-compliant error responses. Include representative cURL or API examples for token exchange and issuer discovery.","requires_code":true,"expected_outputs":["Automated unit/integration tests","Updated configuration and migration documentation","Examples for issuer metadata, JWKS, issuance, and exchange","Documented OAuth error model and HTTP mappings"],"success_criteria":["Tests cover all major success and failure scenarios in scope","Documentation explains configuration, compatibility, trust boundaries, and client auth clearly","Examples are runnable or internally consistent","Error behavior in tests matches documented error semantics"]},{"name":"Run validation, confirm migration readiness, and record follow-up","description":"Execute the full validation pass: tests, manual endpoint checks, compatibility verification against legacy tokens/callers, and rollout-readiness review. Confirm issuer/JWKS rollout order, claim compatibility behavior, and any operational prerequisites. Summarize what was delivered, what remains deferred, and any follow-up items needed for external issuer trust, delegated token profiles, sender-constrained tokens, or fuller revocation semantics.","requires_code":true,"expected_outputs":["Validation report or implementation summary","Compatibility and migration readiness checklist","Documented deferred items and follow-up recommendations"],"success_criteria":["All in-scope tests and manual validations pass","Migration and compatibility behavior is verified against the proposal","Deferred items are clearly separated from delivered functionality","Stakeholders can assess approval readiness from the final summary"]}]}
