{"steps":[{"name":"Repository inspection and capability inventory","description":"Inspect the repository structure, current token issuance and validation code paths, HTTP endpoints, configuration model, policy hooks, signing key handling, and any existing support for JWKS, token exchange, service identities, revocation, or audit logging. Identify the current public contracts and implementation seams so the plan is grounded in the actual codebase rather than assumptions.","requires_code":false,"expected_outputs":["Repository capability inventory","List of existing token-related packages, endpoints, and config surfaces","Gap analysis against required core features","Initial list of reusable extension points and risky areas"],"success_criteria":["Current token and auth flows are mapped end-to-end","Existing public interfaces and dependencies are identified","Core vs optional features can be tied to concrete code locations","Open questions and implementation risks are documented"]},{"name":"Implementation proposal with scope, compatibility constraints, and non-goals","description":"Write a Markdown proposal that converts the repository findings into an executable implementation approach. Define the core required path versus optional/stretch work, record compatibility guarantees, identify known non-goals, and note any migration-sensitive areas. The proposal should explicitly state that sender-constrained support and delegated/envelope tokens are non-blocking unless the repository already supports them naturally.","requires_code":false,"expected_outputs":["Markdown implementation proposal","Core feature list","Optional/stretch feature list","Documented assumptions, non-goals, and repo-specific decisions"],"success_criteria":["Proposal reflects actual repository structure and constraints","Core deliverables are separated from optional items","Compatibility constraints and non-goals are explicitly documented","Proposal is specific enough to guide implementation sequencing"]},{"name":"Backward compatibility and migration assessment","description":"Catalog current API contracts, token shapes, claim names, validation rules, and known client expectations. Determine which behaviors must remain unchanged, which can be extended additively, and where migration notes are needed. Define regression coverage needed to ensure legacy callers and token consumers continue to work after typed claims, context-claim changes, and stricter validation are introduced.","requires_code":false,"expected_outputs":["Compatibility matrix for endpoints, claims, and validation behavior","Migration notes for any renamed or newly enforced fields","List of required regression tests for existing behavior"],"success_criteria":["Existing externally visible behavior is documented","Compatibility guarantees are explicit and testable","Potential breaking changes are identified before implementation","Regression test targets are defined"]},{"name":"Typed token claim foundations and configurable context claims","description":"Implement or refactor shared token claim models so user, service, and exchanged tokens use typed structures with common validation helpers. Keep this step focused on core claim typing, shared issuance/validation logic, and configurable project/context claim naming if needed. Do not include delegated or envelope token work here. Preserve backward-compatible serialization where required by the compatibility assessment.","requires_code":true,"expected_outputs":["Typed claim structs and validation helpers","Shared token profile utilities","Configurable context/project claim handling","Unit tests for claim serialization and validation"],"success_criteria":["Core token types use typed claim models instead of ad hoc maps where appropriate","Common validation rules are centralized","Existing claim compatibility is preserved or explicitly migrated","Tests cover old and new claim shapes as required"]},{"name":"Issuer metadata, JWKS, and key identification","description":"Add standards-oriented issuer metadata and JWKS support based on what fits the repository: OIDC discovery, OAuth Authorization Server metadata, or a minimal issuer metadata endpoint if full standards endpoints are not appropriate. Ensure signing keys expose stable `kid` values and that issued tokens reference the correct `kid`. Include key rotation-friendly behavior if the existing architecture supports it.","requires_code":true,"expected_outputs":["Issuer metadata endpoint or equivalent","JWKS endpoint","Stable `kid` generation/selection logic","Tests for metadata and JWKS responses"],"success_criteria":["Metadata endpoint clearly describes issuer capabilities","JWKS publishes active verification keys in a consumable format","Issued tokens contain valid `kid` headers tied to published keys","Tests verify response structure and key consistency"]},{"name":"Minimal policy decision interface foundation","description":"Introduce the minimum reusable policy interface needed for token minting and token exchange decisions without redesigning the entire policy subsystem. Clarify boundaries: this step provides the decision seam and core inputs/outputs; later steps apply it for project-scoped minting, service issuance, and exchange authorization/narrowing. Reuse existing policy hooks if present rather than replacing them wholesale.","requires_code":true,"expected_outputs":["Policy decision interface(s) and request/response models","Integration points for mint and exchange flows","Basic tests or fakes for policy evaluation wiring"],"success_criteria":["Policy checks can be invoked consistently from mint and exchange code paths","Abstraction is minimal and aligned with existing repo patterns","No unnecessary policy subsystem rewrite is introduced","Tests confirm allowed/denied decisions propagate correctly"]},{"name":"Project-scoped minting rules and regression coverage","description":"Implement business rules for project or tenant context selection during token minting. Enforce allowed scope/context combinations, validate request inputs, and ensure existing mint callers remain compatible. This step should focus on project-context business rules and how they flow into token claims, using the policy interface where needed.","requires_code":true,"expected_outputs":["Updated minting logic for project-scoped tokens","Validation rules for project/context selection","Regression tests for existing mint callers","New tests for allowed and denied project-scoped mint cases"],"success_criteria":["Project-scoped minting behaves according to documented business rules","Existing minting behavior remains intact where required","Invalid context selections fail predictably","Regression tests pass for legacy and new mint flows"]},{"name":"Service identity token issuance","description":"Implement issuance for service or workload tokens with a claim shape distinct from user tokens. Apply audience restrictions, service identity semantics, and any policy checks needed to control which services can mint which tokens. Reuse the typed claim and policy foundations rather than adding parallel logic.","requires_code":true,"expected_outputs":["Service token issuance path","Distinct service-token claim profile","Audience restriction enforcement","Tests for service issuance and validation"],"success_criteria":["Service tokens are distinguishable from user tokens by claim structure or token profile","Audience restrictions are enforced","Policy checks gate service issuance where required","Tests cover positive and negative issuance cases"]},{"name":"RFC 8693 token exchange implementation and error model standardization","description":"Implement token exchange following RFC 8693 semantics using the shared typed claims and policy decision interface. Validate subject token trust, expiration, audience/resource requests, and requested token type handling. If actor tokens are unsupported, reject them explicitly. At the same time, standardize the error model across minting and exchange paths: use consistent HTTP status codes, OAuth-style error bodies where applicable, deterministic validation failures, and sanitized logging for denied or invalid requests.","requires_code":true,"expected_outputs":["Token exchange endpoint or handler","RFC 8693 request validation and response handling","Authorization/narrowing logic for exchanged tokens","Consistent error response helpers","Tests for successful and failed exchange scenarios"],"success_criteria":["Exchange flow supports required RFC 8693 behaviors for the repo scope","Unsupported `requested_token_type`, invalid `audience`/`resource`, expired subject tokens, and unsupported actor tokens are handled explicitly","Client auth/authorization follows existing repository patterns and is concretely enforced","Error responses are consistent, standards-appropriate where applicable, and covered by tests"]},{"name":"Optional delegated or envelope token extension points","description":"Only if the repository already has a clean path for it, implement delegated and/or envelope token support. Otherwise, document typed extension points and defer full implementation. Keep this work isolated from the core claim model so it does not destabilize required delivery.","requires_code":true,"expected_outputs":["Either implemented delegated/envelope support or documented extension-point design","Tests if implemented","Deferral note if not implemented"],"success_criteria":["Feature is only implemented if it fits naturally into existing architecture","Core token flows remain unaffected","If deferred, the reasons and extension points are clearly documented"]},{"name":"Optional sender-constrained token extension assessment","description":"Treat sender-constrained or proof-bound token support as non-blocking future-proofing only. Investigate whether the current architecture naturally supports binding mechanisms; if so, add minimal extension points or placeholders. Do not allow this work to expand into a full proof-of-possession implementation unless the repository already provides substantial support.","requires_code":true,"expected_outputs":["Assessment of sender-constrained feasibility","Minimal extension points or placeholders if appropriate","Deferral documentation if not appropriate"],"success_criteria":["Core delivery is not blocked by sender-constrained design work","Any added scaffolding is minimal and low-risk","If unsupported, the repository has clear notes on how this could be added later"]},{"name":"Audit logging and minimal revocation hooks","description":"Add structured audit logging for issuance and exchange events with sufficient traceability for operators, while keeping sensitive data sanitized. Add minimal revocation-oriented hooks centered on `jti` generation, recording, and extension points for future denylist or revocation integration if the repository does not already have a broader revocation system. Do not expand this into a full revocation redesign unless one already exists.","requires_code":true,"expected_outputs":["Structured audit log events for mint and exchange","`jti` generation and integration hooks","Tests or validations for logging and `jti` presence"],"success_criteria":["Issuance and exchange events are observable with sanitized structured logs","Tokens include `jti` where required","Revocation integration remains minimal and non-blocking","Changes do not introduce sensitive-data leakage"]},{"name":"Tests, examples, configuration samples, and documentation","description":"Produce comprehensive unit and integration tests for new and existing behavior, including regression tests for legacy endpoints and token validation paths. Add examples for metadata discovery, JWKS usage, project-scoped minting, service issuance, and token exchange. Provide configuration samples and operational documentation covering compatibility notes, migrations, and optional features that were deferred.","requires_code":true,"expected_outputs":["Expanded automated test suite","Example requests/responses","Configuration samples","Updated operator/developer documentation"],"success_criteria":["Core and regression tests pass","Examples are runnable or copy-paste usable","Documentation covers setup, behavior, compatibility, and non-goals","Deferred optional features are documented without ambiguity"]},{"name":"Validation run and execution summary","description":"Run the full validation workflow: tests, linting, build, and any repository-specific checks. Summarize implemented features, compatibility outcomes, deferred optional items, known limitations, and follow-up recommendations. This final step should make approval easy by showing exactly what changed and how success was verified.","requires_code":true,"expected_outputs":["Validation results from build/test/lint workflows","Execution summary of completed vs deferred work","Known limitations and recommended next steps"],"success_criteria":["All required validation checks complete successfully or failures are explained with remediation","Implemented scope matches the approved core plan","Deferred items are clearly separated from completed deliverables","Summary provides a concise basis for review and approval"]}]}
